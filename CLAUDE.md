# Unified Stay OS — Claude Code Standing Brief

Read docs/unified-stay-os-spec.md before structural changes. Execute CLAUDE-CODE-P0-PROMPT.md stages IN ORDER; do not advance past a stage with failing tests.

## Non-negotiable invariants
1. Event-sourced writes only. No destructive UPDATE/DELETE on agreement_event, journal_line, action_log.
2. Every agent tool-call runs through App.dispatch's three gates (auth → RBAC → policy) — no bypass paths, ever. Every action that HAS a policy rule passes PolicyEnvelope.decide() BEFORE execution; the config-like surfaces explicitly documented per-module as "RBAC-only, no PolicyEnvelope action" (pricing rules, CRM, roommate, onboarding, budgets, master data, notifications enqueue, party/space setup) are the deliberate, reviewed exception to the policy gate — never to auth or RBAC. Money-moving and regulated actions are ALWAYS policy-gated.
3. Public API is the only API — agents, portal, website consume the same authenticated surface.
4. Double inventory must fail at the DB constraint (calendar_hold EXCLUDE), never only in app code.
5. Policy rules: TypeScript arrays (src/policy-envelope.ts, src/collections.ts) are the single source of truth. Regenerate SQL via scripts/gen-seed.mjs. Never hand-edit supabase/migrations/20260707170001_policy_seed.sql or supabase/policy_rule.seed.sql.
6. Journals must balance; the DB trigger is the enforcement, services assert additionally.
7. Kernel stays zero-runtime-dependency. New deps require written justification in the PR.

## State (as of 2026-07-13)
- Kernel v0.4 complete: 631/631 tests (66 tranches). Run: `npx tsx --test tests/*.test.ts`
- PHASE 2B — LEASE-DOCUMENT GENERATION FEEDING E-SIGN (the draft-the-lease step between an approved application and signature): src/lease-doc.ts is a PURE, zero-dep generator — buildLeaseDocument(terms: LeaseTerms) → LeaseDocument {agreementId, title, parties[], clauses[{heading,body}], jurisdiction, generatedAt} assembling a titled, clause-structured lease (Parties / Premises / Term / Rent / [Security Deposit] / statutory clause / Signatures) that is JURISDICTION-AWARE (BR cites the Lei do Inquilinato 8.245/91 + fiador note; US cites the Fair Housing Act + state security-deposit handling; else a generic governing-law clause); renderLeaseText(doc) flattens to signable plain text with a signature line per party. DETERMINISTIC from the terms → NO storage of its own (regenerated on demand). App.assembleLeaseTerms(tenantId, agreement) folds the deal state into terms: resident/guarantor/cosigner from the party roster (falls back to the master-data guest name), unit label + community, rent, term, the active held deposit, jurisdiction + taxIdLabel from config/country. API: GET /agreements/:id/lease-document (esign.read) → {document, text} (preview, no side effects); POST /agreements/:id/lease-envelope (esign.manage) generates the doc AND opens a DRAFT e-sign envelope, auto-rostering signers from the party links that carry an email (resident→guarantor→cosigner, dedup by party, financial_responsible mapped to 'resident'); 400 if no linked party has an email; default id `env-<agreementId>`, provider defaults docusign; the envelope then flows through the normal POST /signature-envelopes/:id/send path (signing still does NOT execute the lease — lease.execute stays human-gated). AGENT TOOL: draft_lease_envelope (→ POST /agreements/:id/lease-envelope) so the LLM drafts the lease off the agreement, policy-gated like every tool. PORTAL: the agreement-detail Actions gained a "Lease document" panel (esign.read) — "Preview lease" renders the drafted text inline + "Draft e-sign envelope" (esign.manage) opens the envelope (toast → go to E-sign to send). Browser-QA'd (BR lease preview shows the Lei do Inquilinato clause; envelope drafted with the resident signer): NO CONSOLE ERRORS. tranche66 = 11 tests (generator clauses + rent figure, BR-vs-US statutory clause, deposit + guarantor, render text, deterministic, missing-resident throws, GET preview endpoint, resident name from party link, POST rosters signers + drafts + sendable, 400 no-email, read_only previews but can't open). No migration/seed/policy/edge change (reuses esign.read/manage; the doc is regenerated, the envelope persists via the existing signature_envelope table). NEXT in Phase 2: 2C ILS syndication adapter; 2D renewals sweep; 2E tour scheduling.
- PHASE 2A — RENTAL APPLICATIONS + SCREENING (the leasing step the funnel jumped over, between "toured" and "approved"): src/application.ts is a PURE, zero-dep state machine — Application {id,tenantId,leadId?,unitId?,applicantName,applicantEmail?,incomeCents?,status,submittedAt,screening?,decidedAt?,decidedBy?,adverseActionReason?}; status submitted→screening→approved|denied|withdrawn; Applications class (submit/markScreening/recordScreening/approve/deny[reason required]/withdraw/list/get/hydrate; assertDecidable blocks re-decision). SCREENING rides the connector framework — NEW `screening` IntegrationKind (integrations.ts + connector-adapters.ts DISPATCHABLE, NOT a money rail + integration-contract.ts KIND_ACTIONS order_report/get_result, KIND_EVENTS screening_completed/screening_failed); POST /applications/:id/screen enqueues a CREDENTIAL-FREE `screen-<id>` command to the tenant's active screening integration (edge resolves the vendor secret + calls TransUnion/Checkr/…), else just marks screening. The approve/deny DECISION is FCRA / Fair-Housing sensitive → policy-gated (NEW pol-application-decide = allow, ALWAYS audited) + a denial REQUIRES an adverse-action reason and enqueues an `adverse_action` notification (NEW notification kind) to the applicant. API: GET /applications(+/:id), POST /applications (advances the linked lead to 'applied'), POST /:id/screen, POST /:id/screening-result, POST /:id/decide (approve→advance lead to 'approved'; deny→lose lead + adverse-action notice). NEW RBAC perms application.read + application.manage (both in OPS — leasing agents run applications; read_only reads). DURABLE: migration 20260713220000_applications APPLIED LIVE (application table, lead_id/unit_id FKs, screening jsonb, deny-by-default forced RLS); projectWorld + edge twin emit application AFTER crm_lead (13-col, $10::jsonb screening, drift-guarded); snapshotWorld emits applications + rehydrate hydrates + rebuilds applicationTenant map; loadWorld SELECTs them. Seed regenerated (gen-seed → 54 rules) + pol-application-decide APPLIED LIVE (54 confirmed). PORTAL: NEW "Applications" nav view (application.read) — add-application form (name/email/income/unit/lead) + table with Screen/Approve/Deny row actions (Deny prompts for the adverse-action reason). Browser-QA'd (submit → screen → approve): NO CONSOLE ERRORS. tranche65 = 12 tests (submit advances lead, list/read, screen marks + enqueues credential-free command, record result, approve advances lead + audits, deny requires reason + loses lead + adverse-action notice, no re-decide, invalid decision, RBAC + read_only, tenant-scoped 404, projection cols, snapshot→rehydrate). NEXT in Phase 2: 2B lease-doc generation feeding e-sign; 2C ILS syndication adapter; 2D renewals sweep; 2E tour scheduling.
- PHASE 1E — MULTI-GAAP SURFACE (the accrual-vs-cash view finance needs, previously computed but unreachable): src/multigaap.ts gained gaapViewFromLines(lines, basis) (gaapView(ledger, basis) now delegates to it) so the engine works over an explicit tenant-scoped line set. NEW endpoint GET /gaap/:basis (reports.read; basis accrual|cash, else 400) → {basis, totalRevenueCents, revenue{account→cents}} over the tenant's journal lines (agreement-linked OR tenant-tagged). Accrual recognizes revenue at invoice issue; cash proportionally to AR collected. PORTAL: Ledger view gained a "Revenue recognition (multi-GAAP)" panel — accrual vs cash-basis revenue KPI cards. tranche64 = 5 tests (accrual-immediate/cash-nothing, cash-proportional-to-payment, per-account breakdown, invalid-basis 400 + read_only allowed, tenant-scoped). No migration/seed/policy/edge change (pure read over the ledger). NOTE budget variance was ALREADY surfaced (the Purchasing view + GET /budgets fold budgeted/committed/actual/remaining/usedPct via computeBudgetStatus). Phase 1 institutional-books COMPLETE (1A property dim, 1B owner books, 1C period close, 1D trust accounts, 1E GAAP). Remaining smaller follow-ons: monthly budget buckets (per-month actual bucketing); per-property EXPENSE allocation on AP bills (bills carry no propertyId yet, so owner-statement expenses fold to "Unassigned").
- PHASE 1D — TRUST / SEGREGATED DEPOSIT BANK ACCOUNTS (deposit cash must not commingle with operating cash — a jurisdictional requirement): src/bank-account.ts is a PURE, zero-dep tenant-scoped registry — BankAccountRecord {id,code,name,kind:'operating'|'trust',glAccount,entityId?}; trustFor(tenant[,entity]) returns the first (entity-scoped) trust account. A trust account maps to its OWN GL cash account (default assets:cash:trust:<code>). Deposits.hold/refund gained an optional cashAccount (default assets:cash) + entityId/propertyId; the deposit REMEMBERS its cashAccount so the refund returns cash from the SAME account. The App's /deposits handler resolves the tenant's trust account and routes the hold there (+ property/entity stamp). So deposit cash lands in a segregated trust GL, visible separately in the trial balance. API: GET /bank-accounts (entity.read) + POST /bank-accounts (entity.manage; unknown owning entity → 404; a trust account gets its own GL cash by default). PORTAL: Ledger view gained a "Bank accounts" panel (entity.manage) — add operating/trust accounts + a table showing each one's GL cash account. DURABLE: migration 20260713210000_bank_accounts APPLIED LIVE (bank_account table deny-by-default forced RLS + deposit.cash_account column; rolled-back probe green with a bank_account↔deposit join); projectWorld + edge twin emit bank_account AFTER legal_entity + deposit.cash_account (10-col deposit upsert, drift-guarded); snapshotWorld/rehydrate/loadWorld carry bankAccounts + deposit.cashAccount. tranche63 = 8 tests (CRUD + entity.manage gate, no-trust→operating cash, trust→segregated GL, refund from same account, trust balance visible separately, projection cols + order, snapshot→rehydrate keeps routing). Browser-QA'd: NO CONSOLE ERRORS. NEXT in Phase 1: 1E monthly budgets + variance + GAAP surface; + per-property expense allocation on AP bills.
- PHASE 1C — PERIOD CLOSE / POSTING LOCKS (the hard month-end fund-grade books require): src/period-lock.ts is a PURE, zero-dep registry keyed tenant × period ('YYYY-MM') — close/reopen/isClosed/assertOpen/list/hydrate; periodOf(iso)=slice(0,7). ENFORCEMENT at the SINGLE ledger chokepoint: Ledger.setPostingGuard(fn) + post() calls it before staging any entry; the App wires the guard to periodLock.assertOpen, resolving the tenant from the entry's tenantId OR (agreement-linked invoice/payment entries carry no tenantId) via agreementId→this.agreements. So NO invoice/payment/deposit/bill may post into a closed month → LedgerError → 409 (PeriodLockError auto-maps via the *Error→409 convention). API: GET /periods (ledger.read); POST /periods/close (NEW perm period.manage; gated ledger.close_period=ALLOW, audited); POST /periods/reopen (period.manage; gated ledger.reopen_period=ESCALATE → 202, runs the reopen only on human approve — a restatement of books a fund already received). NEW RBAC perm period.manage (manager/staff/accountant/owner; NOT front_desk/agent). NEW policy rules pol-ledger-close-period (allow) + pol-ledger-reopen-period (escalate) — gen-seed → 53 rules, APPLIED LIVE (both confirmed). PORTAL: Ledger view gained a "Month-end close" panel (period.manage) — close a YYYY-MM + a closed-periods table with "Request re-open" (202 toast). DURABLE: migration 20260713200000_period_lock APPLIED LIVE (period_lock PK(tenant_id,period), deny-by-default forced RLS; rolled-back probe green); projectWorld + edge twin upsert period_lock (drift-guarded); snapshotWorld emits periodLocks, rehydrate hydrates, loadWorld reads — a closed period survives restart (posting stays blocked). tranche62 = 9 tests (close→list, invoice+payment blocked in closed month / open month fine, reopen escalates+not-auto / approve reopens, accountant closes but front-desk 403, invalid period rejected, reopen-never-closed refused on approval, snapshot→rehydrate keeps the lock). Browser-QA'd: NO CONSOLE ERRORS. NEXT in Phase 1: 1D trust/segregated deposit accounts; 1E monthly budgets + variance + GAAP surface; + per-property expense allocation on AP bills.
- PHASE 1B — ENTITY/PROPERTY ON JOURNAL LINES + OWNER STATEMENTS (makes the property dimension financially real; institutional fund reporting): JournalLine + JournalEntryInput gained optional entityId/propertyId (ledger.ts; propagated to every line of an entry). billing.issue stamps the invoice's receivingEntityId (charge routing) as entityId + a propertyId; payments.record stamps the SAME entity (read from the invoice) + propertyId; the App resolves propertyId via propertyForAgreement(agreement→currentUnit→propertyId) and passes it into both. So each GL line now books to its owning LEGAL ENTITY and its COMMUNITY. ReportingInput.ledgerLines += entityId/propertyId; ReportingInput += properties[{id,name,entityName?}]. NEW report OWNER STATEMENT (ownerStatement, catalog + BUILDERS): NOI by property/community for the window (revenue credit-normal, expense debit-normal), each row labelled with its owning legal entity — the fund/owner artifact; property-less lines fold to "Unassigned". ?propertyId now ALSO scopes the FINANCIAL statements (income statement/GL) by filtering ledgerLines to that property (added to the reportingInput per-property scope alongside the operational arrays). Report builder `ledger` source gained `property` + `entity` group-by dimensions. Portal: CATALOG_CHART.owner_statement = bar. DEMO seeder now stamps propertyId on its invoices + payments → the seeded owner statement shows real per-community revenue (Praia do Curral R$21,980 / Vila & Perequê R$10,250; NOI R$30,580 matches the income statement); AP-bill expenses still land "Unassigned" (bills aren't property-stamped yet — a 1C+ follow-on). DURABLE: migration 20260713190000_journal_line_entity_property APPLIED LIVE (journal_line += entity_id→legal_entity FK, property_id→property FK, both nullable; ADD COLUMN DDL doesn't trip the append-only UPDATE/DELETE trigger; rolled-back live probe green); projectWorld + edge twin emit entity_id/property_id on the journal_line insert (11-col, drift-guarded statement-identical); loadWorld SELECTs them; snapshotWorld carries them (ledger.allLines) + rehydrate replays verbatim. tranche61 = 9 tests (invoice+payment stamp, owner-statement math + owning entity, catalog, per-property income-statement scope, builder property/entity dims, projection cols, snapshot→rehydrate, condo pass-through routes to a DIFFERENT entity but the SAME property). Browser-QA'd: NO CONSOLE ERRORS. NEXT in Phase 1: 1C period close/posting locks; 1D trust/segregated deposit accounts; 1E monthly budgets + variance + GAAP surface; plus per-property expense allocation on AP bills.
- PHASE 1A — PROPERTY / COMMUNITY DIMENSION (the keystone of the institutional-books phase; a multi-property operator groups units into communities and rolls up per property): PropertyRecord {id,code,name,address?,entityId?} in master-data (MasterData.properties registry; UnitRecord += propertyId?; a property optionally links to an owning legal entity → scopes owner statements in 1B). API: GET/POST/PUT /properties (masterdata.read/manage; id defaults prop-<code>; unknown owning entity → 404); POST/PUT /units + POST /units/bulk accept propertyId (404 unknown; '' unlinks); CSV Import units target += 'property' field (aliases community/building/site/asset…): commit auto-creates the property (prop-<code>) and links every row. REPORTING: ReportingInput.units += propertyId/propertyName; rent roll gained a Property column; GET /reports/:key accepts ?propertyId=<id> which SCOPES operational reports to one community (units → agreements → invoices/payments/deposits/holds all filtered; occupancy/rent-roll/box-score/vacancy/delinquency/lease-expirations become per-property). Financial statements over ledgerLines stay portfolio-wide until 1B stamps entity/property onto journal lines. PORTAL Properties view: "Properties / communities" panel (add: code/name/address/owning-entity + table) + property select on add-unit AND bulk generator + Property column + Properties count card; Reports catalog gained a "Scope:" per-property dropdown (state.reportProperty → ?propertyId=). DEMO seeder: 2 communities (Praia do Curral, Vila & Perequê) with every unit linked, so per-property rollups have real data. DURABLE: migration 20260713180000_properties APPLIED LIVE (property table deny-by-default forced RLS + unit.property_id FK, rolled-back live probe green with a property→SPE→unit join); projectWorld + edge twin emit property AFTER legal_entity + BEFORE unit (FK order legal_entity→unit_type→property→unit) with property_id on the unit upsert (tranche11 drift guard extended, statement-identical); snapshotWorld/rehydrate/loadWorld carry properties + unit.propertyId (same edge-redeploy posture as prior schema adds: repo twin updated, next clean CLI deploy). tranche60 = 11 tests (CRUD+entity 404, unit link/relink/unlink, bulk, CSV auto-create, rent-roll column, per-property rent-roll + occupancy scope, projection order+property_id, snapshot→rehydrate, demo seed). Browser-QA'd end to end: NO CONSOLE ERRORS. NEXT in Phase 1: 1B entity/property on journal lines + per-entity/per-property P&L + owner statements; 1C period close/posting locks; 1D trust/segregated deposit accounts; 1E monthly budgets + variance + GAAP surface.
- PHASE 0 DEFECT SWEEP (5 defects surfaced by the three-director enterprise due-diligence audit): (1) the built-in `accountant` role now includes reports.read + invoice.issue + collections.run (was 403 on every report despite its own "reporting" description; finance can now bill rent + run the sweep) — src/rbac.ts. (2) SEGREGATION OF DUTIES: POST /exceptions/:id/approve now refuses when the approving actor equals the escalation's initiating actor (item.ctx.actor) → 403; a DIFFERENT authorized approver must sign off. The AI agent was already blocked via RBAC; this closes the human self-approval hole (a manager rubber-stamping their own >R$5k payout). Escalations with a system/service initiator are unaffected. (3) lead.source is now reportable: ReportingInput.leads += source; App.reportingInput passes it; the report builder's leads source gained a `source` dimension ('unattributed' when blank) — marketing attribution by channel is now queryable. (4) agreementSummary now carries leaseExecuted (folded from history, no event-scan needed by consumers); rent-roll residentFor falls back to the raw guestId when no party link / master-data guest resolves (API-/CSV-booked agreements show an identifier, not a blank "—"). (5) booking-site unitBaseCents precedence fixed: agreement rate → FLOORPLAN market rent (baseRentCents) → generic pricing-rule base → null, so a nightly dynamic rule no longer masks a multifamily monthly asking rent on the public site. tranche34 + tranche44 approve-flow tests updated to use a distinct approver ('boss'); tranche59 = 8 new regression tests. No migration/seed/policy/edge change (RBAC list + app-layer guards + report dimension only).
- UNIT TYPES / FLOORPLANS + BULK ADD + INLINE DESIGN PREVIEW (operator asks: "the property is 200 apartments units, with different kind, one bed, 2 beds… meant for big companies like Greystar" + "forget the card, just the preview on the website preview"): (A) FLOORPLANS — UnitTypeRecord {id,code,name,bedrooms?,bathrooms?,maxGuests?,areaSqm?,baseRentCents?,description?} in master-data (MasterData.unitTypes registry; UnitRecord += typeId?; snapshot() emits unitTypes). API: GET/POST /unit-types + PUT /unit-types/:id (masterdata.read/manage; numeric fields validated, id defaults utype-<code>); POST/PUT /units accept typeId (404 unknown; '' unlinks). BULK: POST /units/bulk {codePrefix,count≤500,startNumber,typeId?,labelPrefix?} generates code `<prefix><n>` units; codes already in use are SKIPPED (idempotent) → {created,skipped,codes}. CSV Import units target += 'type' field (aliases floorplan/typology/plan…): commit auto-creates the type on first sight (utype-<code>) and links every row naming it — 200 rows → a few types. WEBSITE: BookingSiteInput += unitTypes; SiteUnit += typeId; siteListing emits floorplans[] (SiteFloorplan = type + unitCount + min fromCents across its published units, largest first) and units INHERIT beds/baths/guests/description from their plan (authored unit details always win); unit fromCents and availability quotes fall back to the plan's baseRentCents (market rent). Microsite renders a "Floorplans" section (name, 🛏🛁📐 badges, from-price, N residences). REPORTS: ReportingInput.units += typeName; rent roll gained a Floorplan column. PORTAL Properties view: Floorplans panel (add type: code/name/beds/baths/guests/area/market rent + type table) + type select on add-unit + "⚡ Generate units" bulk form (prefix/first-number/count/floorplan) + Floorplan column + count card. DURABLE: migration 20260713170000_unit_types APPLIED LIVE (unit_type table deny-by-default forced RLS + unit.type_id FK, rolled-back live probe green); projectWorld + edge twin emit unit_type BEFORE unit and type_id on the unit upsert (tranche11 drift guard extended, still statement-identical); snapshotWorld/rehydrate/loadWorld carry unitTypes + unit.typeId (same edge-redeploy posture as branding: repo twin updated, next clean CLI deploy). (B) INLINE PREVIEW — the per-card "Preview ↗" links are GONE; the Website Design section now embeds a LIVE PREVIEW iframe (/site/<tenant>?template=<picked>&radius=&font=&hero=&cards=) that retargets on every gallery click and fine-tune change + an "Open full preview ↗" link; the config preview override now also accepts the 4 fine-tune params (validated via sanitizeSiteContent against the enums). Browser-QA'd end to end (floorplan added, typed unit, bulk 3 units, iframe follows the Tropicália pick, public site shows the Floorplans section): NO CONSOLE ERRORS. tranche58 = 12 tests (CRUD+validation, unknown-type 404, retype/unlink, bulk 200 + idempotent overlap + cap, CSV auto-create, floorplan fold + min price, detail inheritance, availability fallback, rent-roll column, projection order+type_id, snapshot→rehydrate, preview fine-tune params).
- TEMPLATE LIVE PREVIEW (operator ask: "I want to see the templates, the visuals"): GET /site/:tenant/config now accepts ?template=<id> as a PREVIEW override — the listing is themed with that template's defaults (brand accent still applied via resolveTheme(preview, undefined, brand.color)) WITHOUT touching the saved choice; unknown ids are ignored; response carries previewTemplate only when previewing (SiteListing += previewTemplate?). The microsite forwards its own ?template= URL param to the config fetch and, when previewing, renders a fixed bottom ribbon "Design preview: <name> — not saved…". Portal gallery cards each gained a "Preview ↗" link (stopPropagation so it doesn't pick) opening /site/<tenant>?template=<id> in a new tab. Browser-QA'd (20 links, noir preview bg #0d0b0a + ribbon, plain site ribbon-free): NO CONSOLE ERRORS. tranche57 grew 11→12 tests (preview override + junk-id ignored + saved untouched). A 20-shot visual catalog (each template screenshotted on the seeded Ilhabela site via the preview param) was published as a claude.ai artifact for the operator.
- 20 WEBSITE TEMPLATES (operator ask: "create like 20 templates the client can just fill with their properties information… flexible enough to adjust"): src/site-templates.ts is a PURE, zero-dep design registry grounded in web research on the best MF/vacation-rental marketing sites (photo-led hero + booking search above the fold + detail-rich unit cards + trust sections). 20 SiteTemplates, each pure DATA {id,name,description,inspiration,font,radius,hero,cards,palette(bg/card/line/text/muted/accent/accent2 — all hex-6)}: classic(default)/horizon(Airbnb-like)/minima(Sonder)/bluecorp(Blueground)/atlantica/tropicalia(dark green+#ffb703 banner)/metropolitan(charcoal+gold Playfair editorial)/residence/loftworks/nordic/ipanema/vineyard/alpine/urbannest/skyline/noir/palmcourt/hacienda/harborlight/zen(sharp). Axes: 5 HERO_STYLES (classic/banner/split/minimal/editorial) × 3 CARD_STYLES (grid/list/wide) × 3 RADIUS_KEYS (sharp 4/soft 12/round 22 px) × 6 FONTS (system/inter/space whole-page; playfair/fraunces/libre display-only via Google Fonts css2 import). resolveTheme(templateId, options, brandColor) resolves SERVER-SIDE → ResolvedTheme {palette, heroBg, radiusPx, hero, cards, font}: operator templateOptions (radius/font/hero/cards) override the template's defaults (the "flexibility" ask), and the Settings brandColor overrides accent/accent2 (one brand setting flows portal + site + template). templateGallery() → 20 summaries each with a 5-color swatch. SiteContent += template?/templateOptions?/heroPhotoDataUrl? (sanitize drops unknown ids + invalid option values against the imported enums; hero photo via assertSitePhoto ≤256KB). SiteListing += theme (siteListing resolves it per request). API: GET /site-templates (masterdata.read) → the gallery. Microsite: CSS-variable theming (--bg/--card/--accent/--radius/--herobg/--heroimg) + body[data-hero]/[data-cards] variant CSS (banner = photo/gradient bg + overlay; split = 2-col + injected visual; editorial = oversized clamp headline; wide/list card grids) + Google Fonts link injection (display-only fonts style h1/h2/h3/.price only). Portal Website view gained "Design — pick a template": a 20-card gallery (swatch strips + inspiration line, click-to-pick highlight) + fine-tune selects (corners/font/hero/cards) + hero-photo upload; readDesign() folds template/templateOptions/heroPhotoDataUrl into the same PUT /site-content save. Browser-verified (picked Tropicália → public /site/jq renders the dark-green banner theme + authored content): NO CONSOLE ERRORS. tranche57 = 11 tests (20 unique ids, registry integrity, axis variety, fallback, brand override, adjustments, sanitize, gallery endpoint, public theme, end-to-end brand+adjust, snapshot→rehydrate). No migration/seed/policy/edge change (template choice rides the existing tenant.site_content jsonb).
- WEBSITE-BUILDER MODULE + REPORTS v3 (two operator asks: "an entire module that constructs the native integrated website" + "end-to-end reporting covering all areas of the business"): (A) WEBSITE BUILDER — src/site-content.ts is PURE: SiteContent {heroTitle, about, contactEmail/Phone, whatsapp, instagram, facebook, units: Record<unitId, UnitSiteDetails{published, headline, description, bedrooms/bathrooms/maxGuests, amenities[≤24], photoDataUrl(data:image|https, ≤256KB via assertSitePhoto)}>}; sanitizeSiteContent coerces/clamps/drops (about ≤4000ch, numbers clamped, empty→dropped) and THROWS on a bad photo; SiteContentStore is tenant-scoped. API: GET /site-content (masterdata.read) + PUT /site-content (masterdata.manage; SiteContentError→400). bookingSiteInput carries content; siteListing emits page content + per-unit details and — with checkAvailability — filters by isPublished (active AND published!==false; inactive stays hidden regardless of the flag). Portal "Website" is now a BUILDER: link panel + Page-content form (hero/about/contact/social) + a per-unit editor (publish checkbox, headline, beds/baths/guests, amenities CSV, description, photo upload ≤256KB with preview) + one "Save website". The public microsite renders it all: custom hero title, per-unit photo (object-fit cover, gradient fallback), headline, 🛏🛁👥 badges, amenity chips (+n more), clamped description, About section, Contact footer (mailto/tel/wa.me/instagram/facebook), SEO title. DURABLE: migration 20260712180000_site_content APPLIED LIVE (tenant += site_content jsonb); projectWorld + edge twin emit $13::jsonb (drift-guard byte-identical, JSON.stringify), snapshotWorld/rehydrate/loadWorld carry siteContent (same edge-redeploy posture as branding: repo twin updated, live function picks it up on next clean CLI deploy). tranche55 = 11 tests (sanitize, photo 400, publish/unpublish/republish, inactive-stays-hidden, public config carries content, projection + snapshot→rehydrate round-trip, empty-content site still works). (B) REPORTS v3 — ReportingInput += ledgerLines? {account, debitCents, creditCents, postedAt} (App.reportingInput tenant-scopes via the trial-balance predicate). 4 NEW catalog reports (19 total): INCOME STATEMENT (P&L for the window: revenue credit-normal, expense debit-normal, NOI + margin, per-account rows), GENERAL LEDGER (all-time debits/credits/balance per account, net-zero check), BILLED VS COLLECTED (monthly billed/collected/rate + overall collection rate), OCCUPANCY TREND (monthly occupancy, window-clipped months). 2 NEW insights: operating-at-a-loss (window expenses>revenue, metric=NOI) + collection-rate<85%. Report BUILDER gained a 'ledger' source (dims account/category/month; measures net(DR−CR)/debits/credits; category filter). CSV EXPORT on every catalog + built report (client-side Blob download; money cells emitted in major units). Browser-verified on seeded Ilhabela (P&L: revenue $32,230 / expenses $1,650 / NOI $30,580 / margin 94.9% by account; builder-authored site live end-to-end incl. hero/about/contact/amenities): NO CONSOLE ERRORS. tranche56 = 12 tests (P&L math + window exclusion, GL nets zero, collections rate rows/KPIs, trend clipping, both insights + healthy-negative, builder ledger grouping + month sort, seeded endpoints).
- COMPANY BRANDING (logo + accent color + tagline; the operator ask "where do I customize my company / logo"): TenantConfig gained optional brandColor/logoDataUrl/tagline (src/config.ts) + assertBrandLogo() (data:image/* or https, ≤256 KB). PUT /config accepts them on BOTH paths (country-setup + patch), orthogonal to jurisdiction; a bad logo → 400 (explicit HttpError, not the 409 domain-error default); '' clears a field. Applied in TWO places from ONE setting: (a) OPERATOR PORTAL — Settings gained a Branding panel (color picker + logo file→dataURL preview + tagline); render() calls applyBrand() (sets --accent/--accent-2/--accent-soft) and swaps the sidebar mark for the logo <img>; (b) PUBLIC BOOKING SITE — bookingSiteInput carries brand → siteListing.brand → /site/:tenant/config → the microsite applies the accent, logo and tagline. DURABLE: migration 20260712170000_tenant_branding APPLIED LIVE (tenant += brand_color/logo_data_url/tagline, additive+nullable, 3 cols confirmed); projectWorld + the edge twin emit the 3 columns in the tenant upsert (drift-guard tranche11 still 4/4 byte-identical); snapshotWorld emits them, rehydrate reads them, Repositories.loadWorld SELECTs them. The persist-world Edge Function was NOT hand-redeployed (its 31 KB projection twin is updated in-repo + drift-guarded; a live flush ignores the extra tenant fields harmlessly, and rehydrate is separately blocked on DATABASE_URL, so branding persists on the next CLEAN CLI edge deploy — deliberately avoided a risky hand-transcription that could break the whole write path). Browser-verified: set color #e0533a + tagline in Settings → portal nav accent recolors AND the public /site/jq shows the coral CTA/cards + the tagline in the hero; NO CONSOLE ERRORS. tranche54 = 9 tests (logo validation, PUT brand path, orthogonality, bad-logo 400, clear-field, GET /config + public /site config expose brand, projection writes brand_color + snapshot→rehydrate round-trip).
- PROPERTIES VIEW + INTEGRATED BOOKING WEBSITE (two operator-audit gaps): (1) PROPERTIES — units were only creatable via CSV Import or the seeder; added a portal "Properties" nav view (masterdata.read to view, masterdata.manage to add/edit) with an add-unit form + list + activate/deactivate, plus NEW `PUT /units/:id` (masterdata.manage → masterData.units.update label/active). (2) BOOKING WEBSITE — the `website` integration was only a connector seam (push_inventory to an EXTERNAL site); built a real hosted guest-facing storefront over the SAME live inventory. src/booking-site.ts is a PURE engine: siteListing (active units + a "from" price = latest agreement rate on the unit, else the primary pricing rule base, else null) + checkAvailability(from,to) (calendar-overlap exclusion + a per-unit dynamic-pricing quote by running the tenant rule with the unit's base) + isValidDate. PUBLIC (pre-auth, handled in route() before the auth gate, like /auth/config): GET /site/:tenant/config (listing), POST /site/:tenant/availability (live avail+quotes), POST /site/:tenant/inquire (a booking request → a CRM lead source='website' in the operator Pipeline; NO payment/PII, marketing data only). src/api/booking-site.ts = bookingSiteHtml(): a self-contained guest microsite (hero, date picker, unit cards with live price + Available/Booked, request-to-book dialog) served by http.ts at GET /site/<tenant> (exact 2-segment regex; deeper paths are the JSON API). http.ts also now answers GET /favicon.ico → 204 (was a noisy 401 on the auth gate). Portal: NEW "Properties" + "Website" nav views (Website shows the public link + Copy/Open + published unit table). Browser-verified end to end (added "Casa Nova" in Properties → it appears on the PUBLIC /site/jq with its rule-base price; availability shows seeded-booked units as "Booked for these dates" with live quotes on the free ones): NO CONSOLE ERRORS on operator portal AND public site. tranche53 = 12 tests (engine avail/quote/from-price + public endpoints incl. no-bearer + inquire→lead + 404 unknown tenant). No migration/seed/policy/edge change. NOT yet built (operator asks still open): company BRANDING (logo/brand-color — Settings today only has company name/locale/currency/timezone/structure); per-property (vs per-portfolio) site filtering; firm reservation + payment on the public site (v1 is inquiry-only).
- PMS-STAPLE REPORTS (benchmarked against the RealPage/Entrata operational set) + elarapms.com DOMAIN: 6 new catalog reports in src/reporting.ts — RENT ROLL (one row per unit: status occupied/vacant/offline, resident, type, lease start/end, rate, deposit held, outstanding balance; KPIs occupancy %/scheduled rent/outstanding/deposits; currentAgreementByUnit = active AND period contains today), DELINQUENCY (aged receivables BY RESIDENT: current/1-30/31-60/61-90/90+/total buckets per account, sorted by total; KPIs past-due vs total receivable, 90+), LEASE EXPIRATIONS (active agreements bucketed by end month, chronological, holdover bucket; KPIs cumulative ≤30/60/90d counts + rent at risk), BOX SCORE (window move-ins/outs = agreement starts/ends, net change, new leads/signed/lost, occupancy), VACANCY (vacant active units with last-occupied date, days vacant, never-occupied, rent-at-risk = latest agreement rate on the unit), WO AGING (open work orders by days open + priority). ReportingInput extended: agreements += residentName? (resolved in App.reportingInput: party role 'resident' → 'financial_responsible' → master-data guest fullName — via Agreement.guestId public field), workOrders += title?. Catalog reordered staples-first (15 reports). Portal: CATALOG_CHART entries for the 6 (rent_roll/box_score/vacancy tables, delinquency/lease_expirations/wo_aging bars); seriesFromReport now prefers a 'total' column so delinquency charts the headline figure. DOMAIN: elarapms.com is on VERCEL (bought+attached there) — the repo now carries vercel.json with a catch-all rewrite proxying every path to https://elara-tvumsw.fly.dev (the app is a stateful Fly server, not a static site; Vercel 404'd with nothing to serve). Repo has NO main branch — default IS claude/pms-creation-resume-q9kzxs, so Vercel + the Fly deploy workflow both track it. Browser-verified on seeded Ilhabela (rent roll 87.5% occupancy, resident names incl. the guest-fallback 'Beatriz Oliveira'; delinquency 4 aged accounts + bar chart): NO CONSOLE ERRORS. tranche52 = 11 tests. No migration/seed/policy/edge change.
- SELF-SERVICE REPORT BUILDER + CHARTS (upgraded Reports from a fixed catalog into an analytics workbench): src/report-builder.ts is a PURE, zero-dep query engine over the SAME ReportingInput slice — 7 data sources (agreements/invoices/payments/bills/leads/workOrders/deposits) each exposing pickable DIMENSIONS (group-by, incl. time buckets) + MEASURES (count always, plus per-source money sums like billed/collected/outstanding) + FILTERS; buildCustomReport({source,dimension,measure,aggregate:count|sum|avg,chart,filterKey,filterValue,limit}) groups+aggregates into chart-ready {rows, series:{labels,values,valueKind}, kpis, columns} (unit ids relabelled to unit labels; time dims sort chronologically → default line, else desc → default bar). API: GET /reports/build/sources (the picker catalog) + POST /reports/build (run a spec), both reports.read; registered BEFORE /reports/:key so 'sources'/'build' aren't swallowed by the param. Portal Reports view is now a 4-tab workbench: What-needs-attention (insight feed) · Report catalog (each catalog report now renders an inline chart — bar/line/donut picked per report) · Build a report (source/group-by/measure/aggregate/chart/filter dropdowns → live chart + table + KPIs, auto-reruns on change) · My dashboard (saved report widgets). CHARTS are hand-rolled inline (zero-dep, CSP-safe): CSS bar chart, inline-SVG line + donut(+legend). Saved widgets persist in localStorage per tenant (usos.dash.<tenantId>) — "Save to dashboard" from the builder, "✕" to remove; each widget re-runs its spec live. Browser-verified (seeded Ilhabela: donut Portfolio-mix, built "Agreements Count by Kind" bar → saved → dashboard): NO CONSOLE ERRORS. tranche51 = 10 tests (grouping/sum/avg/filter/relabel/time-sort/series-alignment/unknown-source/endpoints). No migration/seed/policy/edge change (reuses reports.read + the in-memory reporting slice).
- REVENUE-MANAGEMENT COCKPIT (upgraded the existing thin "Pricing" view into a real revenue module): computeRevenueInsights(RevenueSignals) in src/revenue.ts is a PURE, zero-dep recommender over demand signals + configured pricing rules — prioritized (critical→positive), explainable opportunities/risks each naming the lever to pull: no dynamic rule at all (warning), rate doesn't respond to occupancy / no weekend uplift / no floor (risk) / no ceiling / no LOS discount (config-gap findings on the primary rule), ADR pressed against the ceiling (opportunity), strong occupancy ≥80% (raise rates) vs soft <40% (stimulate demand), plus a positive note when healthy. API: GET /revenue/insights (revenue.read) → {kpis, insights}. Portal: the nav item is now "Revenue" and viewPricing is a cockpit — KPI header (occupancy/ADR/RevPAR/revenue) + a severity-colored Recommendations feed + a FULL rule editor exposing every lever (base/floor/ceiling, weekend %, two occupancy tiers as at-%/uplift-%, two LOS discounts as nights/discount-%, an optional season window; percents convert to bps client-side, 10000=1.0x) + a Quote simulator that renders a FACTOR WATERFALL (base → each factor with its +/−% and running rate → nightly + total). Browser-verified (seeded Ilhabela: soft-occupancy warning fired; a Dec-25 weekend 5-night quote walked base $450 → occupancy≥85% +35% → weekend +30% → $789.75/night): NO CONSOLE ERRORS. tranche50 = 9 tests (each finding type + determinism + endpoint). No migration/seed/policy/edge change (reuses revenue.read; pricing_rule already persists).
- POST-LOGIN UX BATCH (3 operator asks after first live login): (1) ACCESS ADVISOR — src/access-advisor.ts is a PURE, zero-dep recommender: recommendAccess(description, roleIdHint?) matches a plain-language job description against CAPABILITIES (16 keyword→permission clusters, sensitive-flagged: view/front_desk/payments/refunds/accounting/maintenance/sales/pricing/setup/comms/reporting/collections/approvals/useradmin/privacy/integrations), unions the implied permissions (filtered to the live PERMISSIONS set so it can't drift into invalid grants), finds the closest LEAST-PRIVILEGE built-in role (≤8 extra perms, admin roles only when useradmin is implied) OR proposes an exact custom role, and returns matched capabilities + permissions + suggested/proposed role + rationale[] + risk flags (money-moving/administrative perms) + confidence. The "AI" is a transparent deterministic model (an LLM can re-rank/narrate on the description seam). API: POST /access/advise (role.read). Portal: an Access advisor panel in Users & Roles — describe the person → matched responsibilities + permission chips + suggested/proposed role + risks; "Create this custom role →" pre-fills the custom-role form. (2) REPORTING — src/reporting.ts is a PURE, zero-dep self-service analytics engine: REPORT_CATALOG of 9 reports (occupancy, revenue/ADR/RevPAR, AR aging, collections, deposits, payables, pipeline, portfolio, cashflow) each built over a window from a ReportingInput slice, PLUS computeInsights() — a prioritized (critical→positive), explainable finding feed (unbalanced ledger, 90+/30+ day overdue AR, low/high occupancy, idle units, deposit exposure, overdue payables, stalled leads, urgent work orders, revenue trend vs prior window), each stating the metric it fired on + the action to take. API: GET /reports/catalog, GET /reports/insights, GET /reports/:key (returns {report, insights}), all reports.read; App.reportingInput gathers the tenant slice, App.reportWindow defaults [today-30d, tomorrow). NEW RBAC perm reports.read (in OPS + READS — broadly readable). http.ts now parses ?from=&to= query params into the request body so a GET carries filters. Portal: a Reports nav view (reports.read) — insight feed cards (severity-colored, actionable) + report-picker tabs + KPI tiles + a typed data table. (3) DEMO SEEDER — src/demo-data.ts buildDemoWorld(tenantId, at) is a PURE, deterministic Ilhabela mixed-portfolio generator (9 units incl. one inactive, 8 guests, 10 parties incl. a maintenance vendor + a student's parent-payer, a dynamic pricing rule, 9 agreements across nightly/monthly/lease with mixed statuses, 9 invoices paid/open/overdue/partial, 4 deposits, 3 vendor bills paid/open/overdue, 4 work orders open/in-progress/completed, 6 CRM leads across the funnel incl. one lost). App.seedDemoData(tenantId, at) applies it through the NORMAL kernel stores (Agreement.create + calendar.hold, Billing.issue, Payments.record, Deposits.hold, Payables.issue/pay, WorkOrders, Crm) so invariants hold (ledger balances, no double-inventory) — idempotent via the `demo-unit-ILH-101` marker. API: POST /demo/seed (masterdata.manage) → {seeded, counts}. Portal: a "Load sample data" CTA on an empty dashboard. Browser-verified (owner, empty tenant → seed → Reports → Access advisor): NO CONSOLE ERRORS, ledger Balanced, 9 agreements, occupancy/insights/reports all live. tranche47 (access advisor, 10) + tranche48 (reporting, 11) + tranche49 (demo seed, 11). DURABILITY: no new wiring needed — POST /demo/seed is a mutating 2xx, so the StayServer flush-after-write auto-persists it through the edge backend, and snapshotWorld/projectWorld carry the full seeded world (tranche49 proves it projects to SQL AND survives snapshot→rehydrate: agreements/ledger/overdue-insights all reload intact). Verified against the LIVE DB: tenant `jq` already round-trips its config (Ilhabela/BRL/pt-BR/BR present in the tenant table), confirming durable writes flow in the deployment; seeded data lands on the next redeploy + "Load sample data" click. No migration/seed/policy/edge change (reports.read is RBAC-only; the seeder books through existing gated paths; scripts/qa-new-features.mts is the browser QA).
- GO-LIVE STEP 1 PREPARED (deployment). Everything sandbox-doable is DONE LIVE: migration 20260711180000_schedulers APPLIED (pg_cron + pg_net enabled; 3 active jobs confirmed in cron.job: notification-drain + connector-drain every minute POSTing the edge workers, collections-sweep daily 08:00 UTC POSTing the app) — each job reads its credential from Supabase VAULT at run time (secrets service_role_key / app_url / app_service_token; absent secret → FROM yields zero rows → clean no-op, no hardcoded keys in cron.job). scripts/go-live-acceptance.mjs = zero-dep two-phase acceptance runner (mints its own HS256 owner token for a dedicated golive-probe tenant; `seed` drives config→unit→agreement→invoice→payment→trial-balance→/persist, `verify` after a restart proves rehydration incl. assets:cash=250000) — smoke-tested against a local in-memory server (9/10 checks pass; the durable-flush check correctly fails with "no persistence backend", the expected local outcome). GO-LIVE.md = the operator checklist with real project values prefilled (project URL + PUBLISHABLE anon key only — no secret in the repo); the user's remaining actions: rotate DB password, gather JWT secret + service_role key, fly deploy (single instance), add the 3 Vault secrets, run acceptance seed→restart→verify, create the first Auth user. Providers (SendGrid/Twilio/payment/fiscal) remain bucket-2 on-demand.
- E2E FLOW AUDIT + 7 FIXES (multi-agent audit drove property-ops/finance/resident/platform/agent-tools flows end-to-end — ~550 dispatch checks + portal:drive browser QA; resident/guest flow came back CLEAN, no cross-tenant/cross-party leak, no 500s anywhere, portal NO CONSOLE ERRORS). Fixed: (A) TRANSFER ATOMICITY — the transfer thunk now acquires the TARGET hold FIRST, then appends the event, then releases superseded holds; a double-inventory conflict or a draft-status refusal aborts with NOTHING mutated (was: transferred event persisted + all holds dropped + agreement wedged + old unit double-bookable); same-unit transfer → 400. (B) CALENDAR DATE VALIDATION — Calendar.hold rejects non-ISO date strings (regex + Date.parse + a date-only ROUND-TRIP check since V8 parses '2026-02-30' as Mar 2); garbage dates could defeat the lexicographic overlap check in-memory (invariant 4 must not depend on reaching the DB EXCLUDE). hydrate() stays verbatim. (C) BILL+PO ATOMICITY — Procurement.assertCanBill (pure pre-check) runs BEFORE payables.issue posts GL, so an over-billing/wrong-status PO refusal can no longer leave a booked, payable phantom bill behind a 409. (D) AP LEDGER TENANT-SCOPING — JournalLine gained tenantId?; payables stamps it on bill-/appay- entries (they have NO agreementId, so tenant scoping via the agreement join silently dropped ALL AP activity from /ledger/trial-balance, reporting, snapshotWorld AND loadWorld — AP money history was lost on restart while still reporting balanced:true). snapshotWorld/highWaterMark/trialBalance use the shared predicate (agreement-linked OR tenant-tagged); migration 20260711170000_journal_line_tenant (journal_line.tenant_id + index) APPLIED LIVE + rolled-back probe; projectWorld + edge twin emit the column (drift-guarded); loadWorld LEFT JOINs (a.tenant_id=$1 OR jl.tenant_id=$1, order by jl.id); Repositories.loadTrialBalance same. (E) FLUSH SERIALIZATION — flushWorld chains per-tenant behind an in-flight promise (flushInFlight map); the debounced write-flush racing the periodic safety flush both snapshotted the same delta before either advanced the mark → duplicate journal_line/agreement_event/action_log rows in the DB and doubled balances on next boot. (F) HTTP 413 DELIVERY — readJsonBody pauses (not destroys) on the 1 MiB limit; the 413 (+connection:close) is WRITTEN and flushed, THEN the socket closes (was: ECONNRESET, the documented 413 never reached the client). (G) ESCALATION PERSISTENCE — ExceptionQueue gained all()/hasThunk()/hydrate() (counter resumes past max exc-N); snapshotWorld emits exceptions (tenant-filtered via ctx.tenantId, always upserted for status), projection upserts exception_item (table existed since migration 1, no migration needed), loadWorld reads WHERE ctx->>'tenantId'=$1, rehydrate hydrates — pending human decisions SURVIVE RESTART. A rehydrated item has NO deferred thunk (closures don't persist): POST /exceptions/:id/approve now returns executed:true|false — false = decision recorded + audited, the operator RE-INITIATES the action (it re-escalates with a live thunk); the bill/action is never silently marked done. persist-world Edge Function REDEPLOYED v14 (ACTIVE, deployed body fetched + verified to carry journal_line.tenant_id + the exception_item upsert). Invariant 2 wording CLARIFIED to match the reviewed per-module design (RBAC-only config surfaces are the deliberate exception to the policy gate; money/regulated actions always gated). tranche44 = 14 regression tests. Audit non-defect observations: booking end<=start surfaces as 409 (statusForError maps domain errors to conflict) with the internal hold-id in the message; guestId is not validated against master data (residents are carried by party role links by design).
- NF-e / NFS-e FISCAL EMISSION SEAM (compliance — the outbound counterpart to nfe-ingest). Emitting an electronic fiscal invoice is an OUTBOUND connector command to the tenant's NEW `fiscal` integration kind — same pluggable discipline as locks/banks (kernel enqueues a credential-free command, edge resolves the certificate + calls the provider, credential NEVER in kernel). ADDED `fiscal` to IntegrationKind + INTEGRATION_KINDS (integrations.ts) AND the connector-adapters.ts twin; KIND_ACTIONS.fiscal = emit_invoice/cancel_invoice/get_status + KIND_EVENTS.fiscal = invoice_authorized/invoice_rejected/invoice_cancelled (integration-contract.ts); fiscal is DISPATCHABLE (not a money rail — it emits a document for money that already settled) in connector-adapters.ts + the connector-worker edge twin. generic_fiscal reference adapter (adapter-registry.ts, enabled:true, buildRequest emit_invoice→POST {baseUrl}/nfe bearer, credential-free; body SNAPSHOTS payload to avoid the payload._request cycle — same fix applied to genericWebhookWebsite push_inventory/push_rates). API: POST /invoices/:id/emit-nfe (perm connector.dispatch, gated connector.dispatch) resolves the tenant's active fiscal integration, builds the payload from the invoice (id/agreementId/totalCents/lines + recipient.taxId from the bill-to party), attaches the adapter _request, enqueues id=`nfe-<invoiceId>` (idempotent — re-emit 409); 409 if no active fiscal integration, 404 unknown invoice. Inbound authorization returns via the EXISTING POST /integrations/:id/events (fiscal invoice_authorized recorded on the integration thread). connector-worker Edge Function REDEPLOYED v3 (ACTIVE, fiscal added to DISPATCHABLE_KINDS twin). tranche43 = 13 tests (contract, adapter builds credential-free, routing dispatches fiscal/rejects w/o secret + not money, emit enqueues with recipient taxId + _request + no secret leak, 409 no-integration, 404 unknown, idempotent re-emit, inbound authorization recorded). No migration/seed/policy change (reuses connector.dispatch + the connector outbox tables). Onboarding a fiscal provider = register the integration + drop a vendor adapter; kernel untouched.
- LGPD/GDPR DATA-SUBJECT RIGHTS (compliance — reconciled with the event-sourced ledger). src/compliance.ts = PURE, zero-dep: redactPartyRecord(p,at,reason) tombstones a party (displayName→'[erased]', legalName/taxId/email/phone dropped, attributes→{erased:true,erasedAt,reason} — the marker lives in the jsonb attributes column so NO schema change), isErased(), redactRecipient(), buildSubjectAccessReport (pure assembler over passed slices), ErasureReceipt type. RIGHT OF ACCESS: GET /privacy/parties/:id/export assembles party + agreement roles + invoices billed-to + AP bills payee + notifications + CRM leads + roommate prospects; route permission is NULL so the handler authorizes either an operator with privacy.export OR a party-scoped guest exporting ONLY its own id (else 404). RIGHT TO ERASURE: POST /privacy/parties/:id/erase (perm privacy.manage, policy-gated privacy.erase = allow+audited) redacts the party PII (PartyDirectory.erase) + its notification recipients (Notifications.redactRecipient), and RETAINS the append-only financial record (agreement role links, invoices, bills, journal lines) by opaque party id — the reconciliation with invariant 1: immutable events reference the party by id, so wiping identifying fields de-identifies the subject while the balanced ledger stands. The erasure is recorded in the tenant action_log via gated() (who/when). NEW RBAC perms privacy.export + privacy.manage (owner/service/manager; deliberately NOT `.read` so read_only doesn't inherit bulk PII export; NOT in OPS — front-desk/agent can't erase). NEW policy rule pol-privacy-erase (allow) — seed regenerated (gen-seed → 51 rules) AND APPLIED LIVE (51 confirmed, privacy.erase=allow). PERSISTENCE: the party upsert already carries all columns; the notification upsert's do-update set now also updates recipient+data so a redaction propagates on flush — projectWorld + edge twin updated (drift-guarded byte-identical), persist-world Edge Function REDEPLOYED v13 (ACTIVE), rolled-back probe confirmed the recipient redaction upserts. tranche42 = 13 tests (redaction is idempotent-safe, export assembles, guest self-export only, read_only/front_desk blocked, erasure keeps the ledger summing to 0, audited in action_log, tenant-scoped cross-tenant erase 404, survives snapshot→rehydrate). No migration (attributes jsonb carries the tombstone).
- OBSERVABILITY + RATE LIMITING (production hardening — logs/metrics/error-tracking + abuse bounds). src/observability.ts = zero-dep: Logger (JSON lines to an injectable LogSink, child() context binding, level filter, redacts authorization/token/secret/… field names, throwing sink never propagates), Metrics (labeled counters + latency histograms; snapshot() for JSON, renderProm() for Prometheus text v0.0.4; histogram buckets stored already-cumulative), ErrorReporter seam (LoggingErrorReporter default; inject Sentry/Datadog w/o a kernel dep), defaultObservability({sink,level}) bundle + silentSink. src/ratelimit.ts = zero-dep token-bucket RateLimiter (capacity + refillPerSec, injectable nowMs, per-key buckets keyed tenant+actor, idle eviction), take(key)→{allowed,remaining,retryAfterSec,limit}. WIRED into App: AppConfig.observability?(Partial) + rateLimit?(RateLimiter|{capacity,refillPerSec}); the App is SILENT by default (silentSink — tests/in-process log nothing), main.ts opts into stdout at LOG_LEVEL. dispatch() is now a thin instrumentation shell over a private route(): per request it increments http_requests_total{method,route,status} (route = the PATTERN /agreements/:id/billing, never the concrete path — low cardinality) + observes http_request_duration_ms{route} + logs one structured 'request' line {method,route,status,tenant,durationMs}; a 5xx increments http_server_errors_total and is captured by the ErrorReporter (never leaks a stack). Rate limit runs AFTER auth (keyed tenant+actor) → 429 + Retry-After/X-RateLimit-* headers (http_rate_limited_total{tenant}); an unauthenticated flood is cheaply 401'd (edge/CDN fronts it). gated() counts policy_decisions_total{action,outcome} — the allow/escalate/deny split. ApiResponse gained optional headers (http.ts merges them + writes a STRING body verbatim so Prometheus text passes through). NEW routes GET /metrics (Prometheus text) + GET /metrics.json (snapshot), gated NEW perm metrics.scrape (owner/service/manager; deliberately NOT a `.read` so read_only doesn't auto-inherit a process-wide endpoint, NOT in OPS). main.ts env-wires LOG_LEVEL + RATE_LIMIT_RPS/RATE_LIMIT_BURST. DEPLOY.md gained an Observability section + the 3 env vars. tranche41 = 14 tests. No policy/seed/migration/edge change (pure app-layer cross-cutting).
- NOTIFICATION TRANSPORT (email/SMS) — the outbound messaging arm, same discipline as the connector framework (kernel RECORDS, edge SENDS, credential NEVER in kernel). src/notifications.ts = a PURE, zero-dep outbox: NotificationChannel email|sms, NOTIFICATION_KINDS (collections_reminder/payment_receipt/esign_request/work_order_update/general, each with its data keys) + isKnownNotificationKind, NotificationRecord{id,tenantId,channel,to,kind,data,status,createdAt,sentAt?,failedReason?,providerRef?}, Notifications class (hydrate/enqueue/get/markSent/markFailed/pending/list; enqueue rejects unknown channel/kind/empty-recipient/dup id). App: POST /notifications (notification.send), GET /notifications(+/:id) (notification.read), POST /notifications/:id/{sent,fail} (edge-worker callbacks). NEW RBAC perms notification.read + notification.send (both in OPS). TRIGGERS (best-effort, swallowed on error via App.notify): collections sweep enqueues a collections_reminder to the agreement's bill-to email; e-sign send enqueues an esign_request per signer; a recorded payment enqueues a payment_receipt. emailForAgreement resolves via parties.billTo→getParty.email (no email → skipped, never throws). PERSISTENCE: migration 20260710174000_notifications (notification table, `recipient` column not the reserved `to`, deny-by-default + forced RLS) APPLIED live + rolled-back probe; projectWorld + edge twin emit the upsert (byte-identical, drift-guarded); snapshotWorld emits notifications, rehydrate + loadWorld read them back. persist-world Edge Function REDEPLOYED v12 (ACTIVE, deployed body fetched + confirmed to carry the notification insert). EDGE WORKER: supabase/functions/notification-worker (Deno, DEPLOYED v1 ACTIVE, verify_jwt=true + in-body role=service_role) drains pending rows, renders the per-kind template, sends via SendGrid (email) / Twilio (SMS) resolving SENDGRID_API_KEY/NOTIFICATION_EMAIL_FROM or TWILIO_* from Deno.env (never in kernel/DB), records sent/failed; missing provider config → failed with a clear reason (never silent-sent). Drain SELECT+UPDATE VALIDATED against the live schema (rolled-back probe). Scheduler is deployment infra (cron/pg_cron POSTs /notification-worker with the service role). DEPLOY.md gained a Notifications section + the provider-secret table. tranche40 = 11 tests.
- LOGIN FLOW — a real sign-in in front of the JwtAuthenticator (the portal no longer needs a pasted token in production). GET /auth/config is PUBLIC (special-cased at the very top of dispatch, before auth — the SPA needs it BEFORE it has a token): returns {mode:'supabase',authUrl,anonKey} when AppConfig.authConfig is set (main.ts derives it from SUPABASE_URL+SUPABASE_ANON_KEY → `${url}/auth/v1` + the PUBLIC anon key), else {mode:'dev'}. It exposes NO secret. Portal: boot() fetches /auth/config first; renderLoginGate() shows a real email/password screen (renderPasswordGate) that POSTs GoTrue's password grant (`${authUrl}/token?grant_type=password`, apikey header), stores access+refresh tokens (localStorage), and api() transparently REFRESHES once on a 401 (grant_type=refresh_token) before retrying; a Sign-out button clears the session; dev mode falls back to the paste-token gate. JwtAuthenticator reads tenant_id/user_role/party_id from app_metadata (GoTrue's custom-claims home) — so a real GoTrue JWT authenticates end-to-end. /me now also returns partyId for party-scoped sessions. Browser-verified (portal:drive, NO CONSOLE ERRORS). tranche39 = 6 tests (public /auth/config in dev + supabase modes, other routes still 401, a GoTrue app_metadata JWT authenticates + is tenant/role/party scoped, wrong-secret 401 but /auth/config still reachable). DEPLOY.md gained a Supabase Auth section (app_metadata custom claims via Admin API or an access-token hook; HS256 legacy-secret note). Setup steps for the tenant admin: create the user in Supabase Auth, set app_metadata.{tenant_id,user_role[,party_id]}.
- DEPLOYMENT + DURABILITY LIFECYCLE — the shell that turns the correct in-memory kernel into a durable running service. App.flushWorld(tenantId) = server-internal incremental flush (snapshot delta → persistence backend, advance high-water mark only on success); persist() now gates then delegates to it. App.boot(reader, tenantIds) = cold-start: for each tenant reader.loadWorld()→rehydrate, then set the flush mark to the high-water so the first post-boot flush sends only NEW rows. App.identify(bearer) exposes the authenticator to the lifecycle. WorldReader interface = { loadWorld(tenantId) } (Repositories implements it). src/api/server.ts StayServer: on start() awaits boot (if reader+tenantIds), binds http (createHttpServer gained an onResponse hook), and FLUSHES a tenant durably after each mutating 2xx write on a debounce (flushDebounceMs, default 1500 — bounds the data-loss window) PLUS a periodic safety flush of any still-dirty tenant (default 30s); stop() does a final drain (graceful shutdown). src/api/main.ts = production entrypoint: env-wired (JWT_SECRET REQUIRED → JwtAuthenticator; SUPABASE_FUNCTIONS_URL+SERVICE_ROLE_KEY → edge write backend; DATABASE_URL → PgQueryExecutor reader + tenant enumeration; BOOT_TENANTS/FLUSH_*), SIGTERM/SIGINT graceful drain. Dockerfile (node:22-slim, tsx, optional pg) + .dockerignore + fly.toml (single-instance — in-memory state, scale UP not out) + DEPLOY.md (architecture, env table, live 4-step acceptance). `npm start` runs it. SMOKE-VERIFIED locally: boots, `listening on :PORT`, GET /health → 401 without a token / {ok:true} with a valid HS256 token. tranche38 = 8 tests (flushWorld incremental; flush without backend throws; boot rehydrates + marks high-water; StayServer boots before serving, flushes after a write not after a GET, and stop() final-drains). The one thing NOT runnable in this sandbox (egress-blocked): the live boot→write→restart→rehydrate round trip over real Postgres+edge — documented as the DEPLOY.md acceptance to run from the deployment.
- INTEGRATION PORTS — pluggable vendor onboarding, BOTH directions (the "receiving port per kind" so a new vendor is ONE adapter file, no core change). OUTBOUND: src/integration-contract.ts = the canonical per-kind vocabulary (KIND_ACTIONS + KIND_EVENTS for all 7 kinds; isKnownAction/isKnownEvent/fullContract), vendor-independent. src/adapter-registry.ts = Adapter interface + AdapterRegistry: an adapter is a PURE translator buildRequest(action,payload,config)→VendorRequest (method/url/auth/headers/body) where `auth` is a DESCRIPTOR ({scheme:'bearer'|'header'|'basic'|'none'}) telling the edge how to inject the resolved secret — the credential NEVER enters the kernel. Reference adapters (generic_rest lock/access_control/bank, generic_webhook website); MONEY adapters ship enabled:false (port exists, payout refused until human sign-off — belt-and-braces with planConnectorCommand refusing money kinds at drain). App: GET /integrations/capabilities (+/:id/capabilities); POST /integrations/:id/commands validates the action against the resolved adapter (400 if unsupported) and attaches a CREDENTIAL-FREE _request template. No adapter → still enqueues (back-compat). AppConfig.adapters injects a deployment's real registry. INBOUND (vendor→Elara): POST /integrations/:id/events — NEW perm integration.events (service/webhook only, NOT OPS; the edge verifies the provider signature before relaying); the event must be in KIND_EVENTS[kind] (400 else); routed bank 'transaction_posted'→reconciliation.import, crm 'lead_created'→crm.createLead, everything else recorded on a per-integration `integration-events-<id>` thread; idempotent on the provider's eventId (duplicate→200 no-op). EDGE RUNNER: connector-worker REDEPLOYED v2 (ACTIVE, ezbr_sha256 73328ea4…) — dispatchToVendor now executes payload._request via fetch, injecting the secret per the auth descriptor (bearer/header/basic); no adapter → no-op drain. So a new vendor NEVER touches the edge — only a kernel adapter is added. tranche37 = 23 tests. Onboarding a vendor = drop one adapter file + register the integration + put the key in the vault; kernel/policy/outbox/edge/every other tenant untouched.
- CI + SESSIONSTART HOOK (#9 of the hardening backlog — the "must be N/N before any commit" gate is now automated): (1) .github/workflows/ci.yml runs `npm ci --omit=optional` → `npm run typecheck` → `npm test` on every push + PR (Node 22). (2) package.json gained `typecheck` (tsc --noEmit) and `ci` (typecheck && test) scripts. (3) .claude/hooks/session-start.sh + .claude/settings.json register a SYNCHRONOUS SessionStart hook that `npm install`s devDeps in the remote (Claude Code on the web) env only (guarded by $CLAUDE_CODE_REMOTE), so a web session can run the suite/typecheck immediately without a race. Validated: hook exits 0, typecheck clean, a sample tranche 8/8. The kernel stays zero-RUNTIME-dependency; devDeps are only tsx + typescript + @types/node (pg is optional, omitted in CI).
- COLLECTIONS SWEEP WIRED (#8 of the hardening backlog — the overdue ladder + late-fee math existed but nothing RAN them): App.runCollectionsSweep(ctx, at) + POST /collections/sweep. For every OPEN, past-due invoice (late-fee invoices excluded so a fee is never charged on a fee) it finds the highest stage reached (stageFor) and dispatches that stage's policyAction through the policy envelope: remind/late_fee EXECUTE (a late fee posts a receivable ONCE via a deterministic `latefee-<invoice>` invoice, DR AR / CR revenue:late_fee, feeBps of outstanding), suspend/evict ESCALATE (parked for a human). IDEMPOTENT: each (invoice, stage) is journaled as a message on a per-invoice `col-thread-<invoice>` collections thread, so re-running never double-charges or re-escalates (the escalate branch posts the marker too, so suspend/evict aren't re-parked). NEW RBAC perm collections.run (manager/staff/owner; service via '*'; NOT front_desk/agent). The SCHEDULER is deployment infra — an external cron or Supabase pg_cron (+pg_net) POSTs /collections/sweep on a cadence with the service role; the kernel provides the idempotent operation, the deployment schedules it. `at` is injectable for testing/backfill. tranche36 = 8 tests. No new policy actions (collections.* rules already existed) → no seed/migration/edge change.
- ONBOARDING AGREEMENTS TARGET (#7 of the hardening backlog — the previously-deferred import target): the CSV importer now handles 'agreements' alongside units/guests. TARGET_FIELDS.agreements = code/guestCode/unitCode/kind/start/end/rateCents (with header aliases so an arbitrary export maps via suggestMapping). /onboarding/preview dry-runs it like any target; /onboarding/commit BOOKS a draft agreement per ok row through the SAME event-sourced path as POST /agreements — guest/unit CODES resolve to master-data ids, Agreement.create + calendar.hold (invariant 4 guards double-booking), id=`agr-<code>`. A row whose codes don't resolve, whose kind∉{nightly,monthly,lease}, whose rateCents isn't a positive int, or whose booking trips a constraint is reported as FAILED with reasons (never silently dropped); re-commit skips already-booked contracts (idempotent). Reuses masterdata.manage (no new RBAC/policy/seed/migration; agreement.create is allow). Portal Import view gained the Agreements/leases target with a shape-aware placeholder + a failed-rows count. tranche24 grew 13→18 tests.
- CONNECTOR EDGE ADAPTERS (#6 of the hardening backlog — the runtime drain arm for the connector outbox): src/connector-adapters.ts is a PURE, zero-dep ROUTING POLICY — planConnectorCommand({action,kind,secretResolved,payload}) → {decision, reason}: `dispatch` (a hardware/software kind — lock/access_control/elevator/website/crm — WITH a resolved secret), `refuse` (bank/payment_gateway — a MONEY RAIL, never auto-dispatched: a payout adapter needs explicit human approval; this is defence-in-depth on top of the policy envelope already escalating a large connector payout), or `reject` (unknown kind / missing secret / no action). tranche35 = 8 tests. RUNTIME: supabase/functions/connector-worker (Deno Edge Function, DEPLOYED v1 ACTIVE, verify_jwt=true + in-body role=service_role) drains pending connector_command rows, joins each to its integration for kind+secretRef, RESOLVES the secret from Deno.env / Supabase Vault (NEVER in the kernel/DB — the row holds only a secretRef pointer), applies the routing policy (a verbatim twin of connector-adapters.ts), performs the real vendor call for dispatchable kinds behind a clearly-marked seam (real provider HTTP not wired for vendors we can't authenticate against here), and records dispatched/succeeded/failed back on the command in one tx. The drain SELECT+UPDATE VALIDATED against the live schema (rolled-back probe: a lock command joins its SALTO_KEY, a bank command its ITAU_KEY; routing → lock dispatches, bank refuses). Cannot be invoked over HTTPS from THIS container (egress), so runtime is unverified here; correctness is proven by the kernel routing tests + the live SQL probe. No credential-touching payment code was written (behavioral guardrail).
- LEASE.EXECUTE WIRED (#5 of the hardening backlog — closes the reserved-not-wired residual): POST /agreements/:id/execute-lease dispatches lease.execute. Agreement.executeLease(at) appends a 'lease_executed' event (regulated, irreversible binding — distinct from converting the agreement KIND to 'lease'); requires a lease-kind, active, not-yet-executed agreement; Agreement.leaseExecuted getter folds it. Because pol-lease-execute ESCALATES in EVERY jurisdiction, the endpoint ALWAYS returns 202 (parked) and the binding runs ONLY on human approve() — never auto-executed. NEW RBAC perm agreement.execute (manager/staff/owner; NOT OPS → agents can't initiate). Migration 20260710173000 widened the agreement_event type check to accept lease_executed (APPLIED live + rolled-back probe). Projection passes event types through unchanged → no edge redeploy; lease.execute rule already existed → no seed change. tranche34 = 6 tests (escalates not auto-executes; approval binds; agent 403; non-lease refused on approval; idempotent; survives snapshot→rehydrate). SECURITY.md residual updated to FIXED — ALL security-review residuals now closed.
- JURISDICTION-CHANGE ESCALATION (#4 of the hardening backlog — closes the security review's tenant-mutable-jurisdiction residual): PUT /config still derives jurisdiction from country, but CHANGING an ALREADY-ESTABLISHED jurisdiction now dispatches config.change_jurisdiction, a NEW escalate rule (pol-config-change-jurisdiction) → 202, parked for human approval + audited in the tenant-scoped action_log. So a tenant admin can't silently weaken a jurisdiction-scoped control (e.g. BR→US to escape the deposit cap). First-time setup (ConfigStore.has(tenantId) false — never explicitly configured) applies directly, as does a tweak that doesn't move the jurisdiction. ConfigStore gained has(). Seed regenerated (gen-seed → 50 rules) AND APPLIED LIVE (50 confirmed, jchange=escalate). tranche28 updated (the BR-vs-US demo now uses two SEPARATE tenants instead of flipping one; + a new test asserting first-time-setup applies but an established BR→US change escalates and leaves jurisdiction=BR). Edge projection unchanged (no schema change) → no redeploy. SECURITY.md residual updated to FIXED.
- E-SIGN WEBHOOK HARDENING (#3 of the hardening backlog — closes the security review's forgeable-completion residual): recording a signer's completion (POST /signature-envelopes/:id/sign) IS the provider's webhook, so it now requires a NEW permission esign.complete (held only by owner/service via '*', deliberately NOT in OPS — an operator/agent can no longer forge a signature) AND verifies the request carries the envelope's stored providerRef (the provider's external id set at send); missing/mismatched → 403. esign.manage still governs prepare/send/void (operators). Agent tools unaffected (send_signature is esign.send, no completion tool). Portal sign action re-gated on esign.complete + passes env.providerRef (honestly models the webhook). tranche26 grew 12→14 tests. SECURITY.md residual updated to FIXED.
- GUEST PARTY-SCOPING (#2 of the hardening backlog — closes the security review's guest horizontal-read residual): a party-scoped token (one carrying partyId, i.e. a guest/resident and NOT an operator) may reach ONLY agreements/invoices its party is linked to. Enforced at the single chokepoint App.ownedAgreement (used by GET /agreements/:id, /billing, /parties, /inspections) via callerLinkedToAgreement(partyId, agreementId) = parties.partiesFor(agreementId) contains partyId; the agreement LIST filters the same way; GET /invoices/:id scopes through the invoice's agreementId. Mismatch → 404 (existence never leaked). Operator tokens (no partyId) are unaffected. tranche33 = 5 tests. SECURITY.md residual updated to FIXED.
- PRODUCTION AUTH (#1 of the hardening backlog): src/api/context.ts gained JwtAuthenticator — a zero-npm-dep HS256 JWT verifier over node:crypto (a platform builtin, invariant 7 intact). It verifies the signature in constant time (timingSafeEqual), refuses alg:none / non-HS256, checks exp/nbf (injectable clock + leeway), and derives the AuthContext from claims: tenant_id (REQUIRED — no tenant claim → no access, mirroring deny-by-default RLS, and it reads the SAME claim the DB RLS keys off), user_role→role (falls back read_only), sub→actor, party_id→partyId; each claim resolves top-level OR nested under app_metadata (configurable via claimMap). AuthContext gained optional partyId (the party a guest/resident token acts as — undefined for operator tokens; used by #2 to scope guest reads). StaticTokenAuthenticator stays for tests/demo; both implement Authenticator so JwtAuthenticator drops into new App({authenticator}). tranche32 = 9 tests (valid token, party_id, app_metadata nesting, tampered payload, wrong secret, expired/nbf, alg:none, missing tenant, malformed input never throws).
- SECURITY REVIEW (whole surface: 3 gates + secrets + tenant isolation + injection + persistence) — findings + fixes in SECURITY.md. Architecture holds: no policy bypass (every money/regulated mutation passes gated()→decide() BEFORE effect; agent can't approve its own escalation), agent tools reach kernel only via App.dispatch, SQL fully parameterized (304 placeholders), credentials never enter kernel (secretRef/providerRef only), RLS deny-by-default. FIXED (all live + tested, tranche31 = 7 tests): (1) connector-outbox money rail — NEW rule pol-connector-dispatch-payout escalates a bank/payment_gateway command with payload.amountCents > R$5k (closes the parallel rail around bill.pay); (2) unbounded deposit refund — NEW rule pol-deposit-refund-large escalates net refund (held − deductions) > R$5k; both thread amount/kind into the policy ctx; (3) shallow secret denylist — assertNoSecrets now collapses keys to lowercase-alphanumeric (catches camelCase accessToken/clientSecret), matches substrings, recurses into nested objects/arrays, AND guards connector-command payloads; (4) action_log was NOT tenant-scoped (cross-tenant audit bleed on cold-start) — added action_log.tenant_id (migration 20260710172000, APPLIED live + rolled-back probe), populated from the policy ctx in AgentRuntime, scoped snapshotWorld (actionLogFor(tenantId)) AND loadWorld (where tenant_id=$1); (5) LOW — http.ts now rejects request bodies > 1 MiB with 413. Seed regenerated (gen-seed → 49 rules) AND APPLIED LIVE (49 confirmed, both new escalations present). Edge Function REDEPLOYED to v9 (ACTIVE, verify_jwt=true, ezbr_sha256 7be1f5207dee0af4cd962f7fdc3160f1c4c7cece29041957033c1af25c208ad6) with the tenant-scoped action_log insert (deployed body verified). Residual (documented in SECURITY.md, need design/infra, none an open money-out or cross-tenant WRITE): guest role reads across tenant (latent — no guest tokens minted; needs partyId claim), e-sign completion trusts client email (does NOT execute lease), jurisdiction is tenant-mutable, lease.execute reserved-not-wired.
- Agent tool coverage EXPANDED to the whole platform (invariant 3 — the agent consumes the same policy-gated surface): src/agent/tools.ts grew from 14 to ~31 tools. NEW: get_environment (country env), quote_stay (dynamic pricing), assign_party/adjust_rent/transfer_unit/record_move (agreement lifecycle), create_lead/advance_lead/lose_lead/get_pipeline (CRM funnel), prepare_signature/send_signature (e-sign — does NOT execute the lease), raise_work_order, reserve_space, open_thread/send_message (comms), record_bill (AP issue; PAYING escalates and is not an agent tool). Every tool still routes through App.dispatch → auth → RBAC → policy; NOTHING bypasses. Verified: an agent drives an end-to-end leasing flow (env → quote → lead → book → assign → sign → work order → reserve → message → bill); a BR-jurisdiction deposit cap STILL escalates a tool call (202, not executed); the agent STILL cannot approve its own escalation (403). tranche30 = 10 tests. No new endpoints/policy/RBAC — this is pure surface exposure of the existing gated API to the LLM.
- Cold-start REHYDRATION — durability now round-trips (the write path finally has its inverse): App.rehydrate(world) reconstitutes the FULL in-memory world from a WorldData, the symmetric partner of snapshotWorld. Every store gained a side-effect-free hydrate() (ledger/calendar/billing/payments/deposits/payables/maintenance/reservations/inspection/communications/reconciliation/integrations/connectorOutbox/crm/procurement/esign) + AgentRuntime.hydrateLog; agreements rehydrate from their event stream; parties/spaces/entities pass through verbatim (no explicit-undefined keys); ledger/journal load as stored (NO re-post). Repositories.loadWorld(tenantId) is the DB reader — the inverse of projectWorld — tenant-scoped SELECTs over EVERY table → WorldData, feeding rehydrate on boot; all its SELECTs VALIDATED against the live schema (0 column errors). ACCEPTANCE = a ROUND TRIP: drive a rich world (config/parties/spaces/agreement+ledger/invoice/deposit/AP/pricing/PO/budget/lead/prospect/envelope/integration/command/custom-role) into App A → snapshotWorld → rehydrate a FRESH App B → assert B.snapshotWorld deepEqual A's (nothing the persistence layer captures is lost); plus a loadWorld→rehydrate path proven with a seeded executor. tranche29 = 11 tests. MASTER-DATA FIDELITY (the last lossy edge, now CLOSED): unit.code/active + guest.code/email ARE persisted — migration 20260710171000_master_data_fidelity APPLIED live (unit +code/+active, guest +code/+email, backfill code=id), and projectWorld + the edge twin now emit those columns (unit insert carries code/active with on-conflict update; guest insert carries code/email), snapshotWorld emits them, App.rehydrate reads them, loadWorld SELECTs them. VERIFIED live (rolled back): a unit with code='RIO-101'/active=false and a guest with email persisted then ROLLBACK (DB pristine). Edge Function REDEPLOYED to v8 (ACTIVE, verify_jwt=true), deployed body confirmed to carry the new columns. So NOTHING the kernel produces is dropped on the write/read round trip. So: write (snapshot→edge→DB) AND read (DB→loadWorld→rehydrate) are both complete and lossless; the app survives a restart with nothing lost.
- Per-country ENVIRONMENTS over one identical structure (the architectural keystone): src/environment.ts — MASTER_DATA_STRUCTURE is a single constant (legal_entity, party, space, charge_type, unit, guest, agreement) — being ONE constant, not parameterised by country, IS the guarantee the structure never forks. buildEnvironment(country, overrides?) → EnvironmentBlueprint {country, jurisdiction, taxIdLabel, config (currency/locale/timezone/businessStructure), policy (the EFFECTIVE jurisdiction-scoped rule set), collectionStages, masterDataStructure} — pure/deterministic, so provisioning is reproducible. Provisioning BR vs US = SAME migrations + SAME kernel, different blueprint; NO per-country schema fork. POLICY now supports per-jurisdiction divergence from ONE source: PolicyRule gained `jurisdictions?` (undefined = global; set = applies only in those jurisdictions); PolicyEnvelope.decide() skips a rule whose jurisdictions exclude ctx.jurisdiction; effectiveRulesFor(jurisdiction) slices the global set. DEMONSTRATIVE rule pol-deposit-hold-cap (jurisdictions BR/EU, escalate when amount_cents > 900000) proves it: a >R$9k deposit ESCALATES for a BR/EU tenant but is ALLOW for US — same action, same amount, different environment. lease.execute stays escalate in EVERY jurisdiction (regulated everywhere). Seed regenerated (gen-seed → 47 rules) AND APPLIED LIVE (47 confirmed, cap=escalate, lease.execute=escalate); the jurisdiction is encoded in the seed's condition_note (runtime decisions use the TS rules). API: GET /environment (config.read → this tenant's blueprint). scripts/provision-country.mjs <CC> prints a country's full setup (identical migration list + config + effective policy + collections) — verified BR (47 effective, 1 jurisdiction-scoped, CNPJ/CPF/BRL) vs US (46 effective, 0 scoped, EIN/SSN/USD). Portal: dashboard subtitle shows "<country> env (<jurisdiction>)". tranche28 = 12 tests. So: every country gets its OWN setup + config + jurisdiction-scoped policy; the master-data STRUCTURE is byte-for-byte identical everywhere.
- Country environments + FULL persistence (nothing lives only in memory): src/country.ts — COUNTRY_PROFILES (BR/US/PT/ES/MX/GB) each fixing currency/locale/timezone/JURISDICTION/taxIdLabel. A country is CONFIG + POLICY ONLY — the master-data STRUCTURE (party/space/legal_entity/charge_type/unit/guest) is IDENTICAL for every country, so one kernel + one schema deploys per country (or multi-tenant across countries) with NO data-model fork. TenantConfig gained country + jurisdiction (jurisdiction ALWAYS derived from country, never set alone); ConfigStore.setupForCountry seeds currency/locale/timezone from the profile unless overridden. App.gated() now rides the tenant's jurisdiction into the PolicyContext, so a rule may be jurisdiction-scoped without a code fork (lease.execute stays escalate everywhere for now). API: GET /countries; PUT /config accepts a country (re-derives jurisdiction, seeds defaults). Portal: setup wizard has a Country picker that seeds currency/language/timezone — browser-verified NO CONSOLE ERRORS. tranche27 = 10 tests. PERSISTENCE: migration 20260710170000_config_and_full_persistence APPLIED live — tenant table gained display_name/locale/currency/timezone/business_structure/country/jurisdiction columns (7 cols confirmed), and 5 NEW tables (app_user, custom_role[perms jsonb], integration[config jsonb], connector_command[payload/result jsonb], signature_envelope[signers jsonb]) all deny-by-default + FORCED RLS (rls_on=5). projectWorld + edge twin now emit tenant config + those 5; snapshotWorld carries users/customRoles/integrations/connectorCommands/signatureEnvelopes (all upserted). Drift guard (tranche11) extended + STILL byte-identical. VERIFIED live (rolled back): a BR-config tenant + user + custom role (perms jsonb) + salto integration (config jsonb) + succeeded connector command + signed envelope (signers jsonb) applied cleanly then ROLLBACK (0 rows, DB pristine). Edge Function REDEPLOYED to v7 (ACTIVE, verify_jwt=true) and its deployed body FETCHED + confirmed to contain all 5 new-table inserts + the tenant-config columns (no drift). So the deployed write arm covers EVERYTHING the kernel produces.
- E-signature for lease execution (#17, the CRM → lease → e-sign tail): src/esign.ts — a PURE, zero-dep SignatureEnvelope state machine: create(draft) → send(sent) → recordSigned per signer → when ALL signed → 'signed'; decline(any signer → 'declined'); void (draft/sent → 'voided'). A signer roster carries name/email/role/partyId; email matching is case-insensitive; duplicate emails rejected. The provider (DocuSign/Clicksign/…) is reached from an EDGE ADAPTER that resolves a secretRef — NO credential enters the kernel (same discipline as the connector framework). CRUCIAL SAFETY: a fully signed envelope advances the linked CRM lead to 'signed' (the SALES outcome) but does NOT execute the lease — lease.execute stays an ESCALATE policy rule (regulated/irreversible, human-confirmed). API: POST /signature-envelopes (esign.manage), POST /:id/send (esign.manage → gated 'esign.send' policy, allow+audited), POST /:id/sign ({email} or {email,decline,reason}; on full signature advances the lead), POST /:id/void, GET /signature-envelopes(+/:id). New policy action esign.send (allow) — seed regenerated (gen-seed → 46 rules) AND APPLIED LIVE to shplrbhwpttsukwgaxli (esign.send=allow, lease.execute STILL escalate confirmed). New RBAC perms esign.read + esign.manage — BOTH in OPS (leasing agents send/sign; read_only reads; NOT accountant). An envelope may link a leadId/agreementId (404 if unknown). Portal: an E-sign nav view (prepare envelope with resident+optional guarantor → Send → per-signer Sign / Void row actions) — browser-verified NO CONSOLE ERRORS (a DocuSign envelope sent then signed 1/1 → 'signed'). tranche26 = 12 tests. WIRED: signature_envelope DB table (migration 20260710170000) + projection (upsert; drift-guarded; live-probed rolled back; edge v7).
- Feature persistence wire-up (revenue #1, procurement #2, roommate #9, crm #20 now DURABLE): migration 20260709171000_pms_feature_tables APPLIED live — pricing_rule, purchase_order (lines jsonb), budget, roommate_prospect (preferences jsonb), crm_lead (stage_at jsonb), all deny-by-default + FORCED RLS (rls_on=5 confirmed). projectWorld + the edge twin now emit all five (upsert on id; PO/prospect/lead reference party, PO references legal_entity — emitted after those parents). App.snapshotWorld carries pricingRules/purchaseOrders/budgets/prospects/leads (all upserted, safe to resend). Drift guard (tranche11) extended with all five and STILL byte-identical. VERIFIED live (rolled back): a full feature world — pricing rule (jsonb tiers), approved PO (jsonb lines, total 200000), budget, prospect (chronotype early), toured lead — applied cleanly to shplrbhwpttsukwgaxli then ROLLBACK (0 rows, DB pristine). Edge Function REDEPLOYED to v6 (ACTIVE, verify_jwt=true) — projection now covers all 5 feature tables. Cannot be invoked from THIS container (egress), so v6 is unverified at runtime here; correctness is proven by the drift test + live rolled-back projection probe.
- CRM & pipeline KPIs (#20): src/crm.ts — a PURE, zero-dep leasing sales funnel + the KPI rollups a dashboard needs. A Lead flows new → toured → applied → approved → signed (PIPELINE ladder; forward-only, skipping ahead allowed) or is lost(reason) from any open stage; advance()/lose() stamp stageAt timestamps, closed leads reject further moves. crmKpis(leads) folds the funnel: byStage counts, openCount/wonCount/lostCount, pipelineValueCents (sum est of OPEN leads), wonValueCents (signed), conversionPct (won / (won+lost)). Config/reporting layer only — advancing to 'signed' records the SALES outcome; the actual lease execution still runs through the agreement + lease.execute policy path, so NO money/regulated action here (RBAC-only, NO PolicyEnvelope action, NO seed change). External CRMs (#17 Salesforce/Zoho) feed leads via the connector framework; this is the canonical funnel. API: POST /leads (crm.manage), POST /leads/:id/{advance,lose}, GET /leads(+/:id), GET /crm/summary (KPIs). New RBAC perms crm.read + crm.manage — BOTH in OPS (agent/front-desk are sales; read_only reads via READS; NOT accountant by default). A lead may link a partyId (404 if unknown). Portal: a Pipeline nav view (add lead → funnel table with →next-stage / Lose row actions + KPI cards) PLUS Pipeline KPI cards (open/pipeline value/conversion/won) on the dashboard beside the Revenue cards — browser-verified NO CONSOLE ERRORS (a $3k lead advanced new→toured, KPIs live). tranche25 = 12 tests. WIRED: crm_lead DB table (migration 20260709171000) + projection (upsert; snapshotWorld emits leads; drift-guarded; live-probed rolled back).
- CSV + AI onboarding importer (#15): src/onboarding.ts — a PURE, zero-dep migration pipeline (portfolio onboarding is the usual go-live blocker). parseCsv() is a from-scratch RFC-4180-ish parser (quoted fields, "" escaped quotes, CRLF/LF, header row split from data). TARGET_FIELDS defines canonical fields per target (units: code*/label; guests: code*/fullName*/email). suggestMapping(target, headers) is the deterministic heuristic (exact alias then substring, first header wins) — the "AI migration" seam is exactly this column MAPPING (an LLM can propose/correct it; the App layers a caller-supplied mapping OVER the heuristic, LLM wins where provided). planImport() is a pure DRY-RUN: applies the mapping per row, validates required fields, flags in-file duplicate codes, defaults a unit label to its code → per-row ok/error + counts, writing NOTHING. API: POST /onboarding/preview (dry-run plan) + POST /onboarding/commit (applies ok rows to MasterData units/guests, id=`unit-<code>`/`guest-<code>`, SKIPS codes already present → idempotent re-import); both reuse masterdata.manage (config-like; NO new RBAC perm, NO PolicyEnvelope action, NO seed change). App.parseOnboarding shared-parses + merges mapping. Portal: an Import nav view (pick units/guests → paste CSV → Preview shows the column mapping chips + per-row ok/error plan → Commit) — browser-verified NO CONSOLE ERRORS (2 units created, 1 bad row rejected, re-import skips). tranche24 = 13 tests. NOT yet wired: agreements/leases import target (units+guests only for now; deferred).
- Student roommate matching (#9): src/roommate.ts — a PURE, zero-dep, deterministic compatibility engine for shared/student housing (the space tree already models room→bed). compatibility(a,b) scores how well two prospects would share a room (0..100) from lifestyle preferences (cleanliness 1..5, social 1..5, chronotype early/late/flexible, smoker), penalising distance per dimension with an explainable MatchFactor breakdown; HARD deal-breakers (smokeFreeOnly vs smoker, petFreeOnly vs hasPet) force compatible=false / score 0 (symmetric). RoommateMatcher is tenant-scoped prospect storage + matchesFor(target) (ranked desc by score, then id — deterministic) + suggestGrouping(capacity) (greedy: seed lowest id, pull highest-avg-compatibility members, never group a deal-breaker pair, fill rooms of N beds). The "AI matching" seam is exactly this score — an ML model can re-rank; placement itself reuses the existing agreement booking + unit-transfer (#23) paths, so this module only RANKS. API: POST /prospects (roommate.manage), GET /prospects(+/:id incl. its matches) (roommate.read), GET /prospects/:id/matches, POST /roommate/grouping ({capacity} → groups). RBAC-ONLY gated (config-like pure computation, NO PolicyEnvelope action, so NO gen-seed/policy-seed change). New RBAC perms roommate.read + roommate.manage — BOTH in OPS (front desk/agent handle student placement; read_only reads via READS). Portal: a Roommates nav view (add prospect with lifestyle prefs → prospect table → per-prospect ranked matches → suggest room groups by beds-per-room) — browser-verified NO CONSOLE ERRORS (two identical-preference prospects scored 100%, grouped into one room at avg fit 100%). tranche23 = 12 tests. WIRED: roommate_prospect DB table (migration 20260709171000) + projection (upsert; drift-guarded; live-probed rolled back).
- Purchase orders & budgets (#2 — accounting depth beyond AR/AP): src/procurement.ts — a PURE, zero-dep kernel. A PurchaseOrder is an ENCUMBRANCE, NOT a journal entry: raise (draft) → approve → receive → close/cancel posts NOTHING to the ledger (the AP bill is the only GL-hitting side — inv 6 stays with Payables). committedForAccount() sums OPEN (approved/received) PO line amounts for an account, bucketed by expectedAt (fallback createdAt), released on close/cancel. recordBilling() accrues billedCents and auto-closes a PO when fully billed. A Budget is a planned figure per GL account + [periodStart,periodEnd); computeBudgetStatus(budget, committed, actual) folds budgeted/committed(open POs)/actual(posted bills) → remaining + usedPct + overBudget — the classic plan-vs-commitment-vs-actual variance. API: POST /purchase-orders (+ /:id/{approve,receive,close,cancel}), GET /purchase-orders(+/:id); POST /budgets, GET /budgets (each with folded status), GET /budgets/:id/status. Bill issuance accepts an optional poId → App records the bill total against the PO's commitment. GATES: PO mutations pass the PolicyEnvelope (NEW actions purchase_order.{raise,approve,receive,close,cancel}); **approving a PO > R$5,000 ESCALATES (202)** like bill.pay — so the policy seed WAS regenerated (gen-seed → 45 rules) AND APPLIED LIVE to shplrbhwpttsukwgaxli (the 6 purchase_order rules confirmed in policy_rule). Budgets are config-like → RBAC-only, no policy action. New RBAC perms procurement.read (OPS; read_only reads) + procurement.manage (manager/staff/accountant/owner). App.actualForAccount folds posted (non-void) bill lines; App.budgetStatus joins committed+actual. Portal: a Purchasing nav view (raise PO → approve/receive/close/cancel row actions; set budget → table with budgeted/committed/actual/remaining + a usage bar) — browser-verified NO CONSOLE ERRORS (a $2k approved PO's commitment flowed into an $8k-remaining budget). tranche22 = 13 tests. WIRED: purchase_order + budget DB tables (migration 20260709171000) + projection (upsert; drift-guarded; live-probed rolled back).
- Revenue management & dynamic pricing (#1): src/revenue.ts — a PURE, zero-dep, deterministic pricing engine. computeQuote(rule, ctx) turns a base rate into a nightly + total quote by applying explainable, multiplicative factors in BASIS POINTS (10000 = 1.0x): occupancy tier (highest matching minOccupancyPct wins), weekend uplift (Fri/Sat check-in), lead-time tier (tightest matching maxDaysOut wins), season window ([start,end)), length-of-stay discount (highest matching minNights), then clamp to min/maxCents — every factor pushes a QuoteFactor so the quote is auditable. The "AI/demand" seam is the occupancyPct signal fed in (an ML model can supply it; the arithmetic never guesses). revenueKpis() = the three headline hospitality metrics (occupancy% / ADR / RevPAR). RevenueManagement class is tenant-scoped rule storage + quote(). API: POST /pricing-rules (revenue.manage), GET /pricing-rules(+/:id) (revenue.read), POST /pricing/quote (revenue.read), GET /revenue/summary (revenue.read — App.revenueSummary folds tenant agreements' period-nights [sold], unit count × booked-window span [available], collected invoice cash [revenue] into the KPIs). RBAC-ONLY gated (config-like: NO PolicyEnvelope action, so NO gen-seed/policy-seed change). New RBAC perms revenue.read (OPS; read_only reads) + revenue.manage (manager/staff/owner). Portal: a Pricing nav view (create rule → list → quote-a-stay form showing nightlyCents + factor chips) + Revenue KPI cards (occupancy/ADR/RevPAR/revenue) on the dashboard — browser-verified NO CONSOLE ERRORS. tranche21 = 12 tests. NOTE: `Quote`/`QuoteFactor` were renamed to `PricingQuote`/`PricingFactor` in revenue.ts to avoid a barrel collision with rate-plan.ts's `Quote`. WIRED: pricing_rule DB table (migration 20260709171000) + projection (upsert; drift-guarded; live-probed rolled back).
- Connector framework (the shared spine for #7 door locks, #12 access control, #13 elevators, #14 banks/payment routing, #16 website, #17 CRM): src/integrations.ts — Integrations registry (kind lock/access_control/elevator/bank/payment_gateway/website/crm + provider + NON-SECRET config + a secretRef; register() REJECTS secret-like config keys so a credential never reaches the kernel/DB — the boundary that keeps inv 7 + security intact) and ConnectorOutbox (enqueue a command → pending; an edge adapter that holds the credential markDispatched → markResult succeeded/failed). API: POST /integrations (+ /:id/status), GET /integrations; POST /integrations/:id/commands (enqueue), GET /connector-commands, POST /connector-commands/:id/{dispatch,result} (edge-worker callbacks); gated integration.configure + connector.dispatch. RBAC integration.read + connector.dispatch in OPS (front desk & agent may unlock doors); integration.manage = manager/staff/owner. Portal Integrations nav view (connect a provider, toggle status, send a command, watch the outbox). tranche20 = 11 tests. Design: real vendor I/O lives in edge adapters that resolve secretRef against the secret store (Supabase Vault/env); the kernel only records config + policy-gated commands. WIRED: integration + connector_command DB tables (migration 20260710170000) + projection (upsert; drift-guarded; live-probed rolled back; edge v7) so an edge worker drains the outbox across restarts.
- Phase 2 module 5 — Bank reconciliation (#5, AR & AP): src/reconciliation.ts (Reconciliation: import bank transactions [signed: + inflow / − outflow], match to a payment or ap_payment, unmatch/ignore, summary. suggestMatches() is a PURE deterministic ranker — exact signed amount + date proximity — the "AI-powered" seam an LLM can re-rank on top; zero-dep, reproducible). API: POST /bank-transactions (+ /:id/{match,unmatch,ignore}), GET /bank-transactions (+ /:id incl. ranked suggestions), GET /reconciliation/summary; gated recon.import/match. App.matchCandidates folds AR payments (+) and AP payments (−) for a tenant. RBAC reconciliation.read/manage — a FINANCE function (manager/staff/accountant; NOT agent/front-desk; read_only reads). tranche19 = 11 tests. NOT yet wired: bank_transaction DB table + projection + portal (deferred).
- Phase 2 module 4 — Communications (#4): src/communications.ts (Communications: threads kind resident/finance/internal, optionally scoped to an agreement/party; append messages authored by party/user/agent with inferred direction inbound/outbound/internal; resolve/reopen). An agent-authored send passes the policy envelope like any agent action — that is the "AI communications" surface (LLM drafting is an integration outside the zero-dep kernel). API: POST /threads (+ /:id/messages, /:id/resolve, /:id/reopen), GET /threads(+/:id incl. messages); gated comms.open/send/resolve. RBAC comms.read/send/manage (OPS; agent may send; read_only reads). tranche18 = 10 tests. NOT yet wired: threads/messages DB tables + projection + a portal Inbox view (deferred to the wire-up batch).
- Phase 2 module 3 — Move-in/out (#19/#18) + inspections: Agreement gained moveIn()/moveOut() (moved_in/moved_out events, active-only). src/inspection.ts (Inspections: schedule a move_in/move_out vistoria on an agreement/space → complete with a per-area condition checklist + damageCents → the damage estimate informs the move-out deposit refund via the existing deposit path). API: POST /agreements/:id/{move-in,move-out} (gated agreement.move), POST /inspections (+ /:id/{complete,cancel}), GET /inspections(+/:id), GET /agreements/:id/inspections; gated agreement.move + inspection.create/complete/cancel. RBAC agreement.move + inspection.read/manage (OPS). Portal: move-in/out buttons on the detail. tranche17 = 10 tests. NOT yet wired: inspection DB table + projection (deferred; move events already flow via agreement_event).
- Phase 2 module 2 — Common-area reservations (#6): src/reservations.ts (Reservations over the shared Calendar: reserve a bookable common/amenity space for a window → takes a calendar hold keyed resv-<id>, so overlaps are rejected by the same no-double-booking guarantee as stays (inv 4); cancel releases the hold; carries an optional price). API: POST /reservations (+ /:id/cancel, GET list + /:id), gated reservation.create/cancel; only common/amenity spaces bookable (400 otherwise). RBAC reservation.read/manage (OPS; read_only reads). Portal Reservations nav view (reserve + cancel). tranche16 = 8 tests. NOT yet wired: a reservation DB table + projection (reservation calendar holds are space-scoped, so they persist via calendar_hold.space_id at wire-up — deferred with the batched edge redeploy).
- Phase 2 module 1 — Maintenance / work orders (#3): src/maintenance.ts (WorkOrders: a work order raised on a space, assigned to a vendor party, worked, completed/cancelled; mutable-status record like invoice/deposit with per-transition timestamps; a repair cost reuses AP — complete() can link a bill). API: POST /work-orders (+ /:id/{assign,start,complete,cancel}, GET list + /:id), all auth→RBAC(maintenance.read/manage)→policy (work_order.open/update/close, all allow) gated. New RBAC perms maintenance.read/manage (OPS + manager; read_only reads). tranche15 = 9 tests. WIRED (module-1 wire-up): work_order DB table (migration 20260708171000, deny-by-default+forced RLS) + projection (upsert; snapshotWorld includes it; drift guard covers it; live-probed on the DB rolled back) + portal Maintenance nav view (raise WO, assign vendor, start/complete/cancel — browser-verified no console errors). Edge Function REDEPLOYED to v4 (ACTIVE) — projection now covers work_order + all Phase-2 tables (the deployed projection.ts is a whitespace-compacted equivalent of the drift-guarded repo twin, producing identical statements; repo is source-of-truth). Cannot be invoked from THIS container (egress), so v4 is unverified at runtime here; correctness is proven by the drift test + live rolled-back probes.
- Phase 2 persistence wire-up (modules 1-5 durable): migration 20260709170000_phase2_tables APPLIED live — reservation, inspection (items jsonb), message_thread, message, bank_transaction, all deny-by-default+forced RLS. projectWorld + the edge twin now emit all five (reservation persists as its own record; its calendar hold stays an in-memory mirror, not stored, to avoid the calendar_hold.unit_id→unit FK on a space). App.snapshotWorld carries reservations/inspections/messageThreads/messages/bankTransactions; message is append-only (on conflict do nothing), the rest upsert. VERIFIED live (rolled back): a full Phase-2 world — reservation, completed inspection (jsonb items, damage 8000), thread + message, bank txn — applied cleanly. Portal: Inbox (open thread → messages → reply → resolve) + Reconcile (import bank line, summary cards, auto-match/ignore) nav views — browser-verified NO CONSOLE ERRORS.
- Phase 1 (leasing core on the v2 model) BUILT through the Public API — every write still passes auth → RBAC → policy: (a) Agreement aggregate gained rent_adjusted (#8, escalatedRate() percent/fixed/cap helper + adjustRent()) and transferred (#23, currentUnitId folds transfers; App swaps the calendar hold) events; (b) invoices are charge-routed — a line's chargeCode resolves via EntityCatalog to its gl_account + receiving entity, stamped on the invoice with billToPartyId (payer, from PartyDirectory.billTo); one-invoice-one-entity enforced (400 on mixed); condominium pass-through (#11) books condo_fee to liabilities:due_to_condominium, never operator revenue; (c) accounts payable end-to-end (POST /bills, /bills/:id/pay, GET /bills) with payee=party — a resident refund is a bill to the resident-payee (#22); bill.pay > R$5k escalates (202). New endpoints: /parties, /agreements/:id/parties, /spaces, /legal-entities, /charge-types, /agreements/:id/{adjust-rent,transfer}, /bills(+/:id/pay). New RBAC perms: agreement.adjust/transfer, party.*, space.*, entity.*, bill.read/issue/pay (agent may issue bills but not pay; manager/accountant may pay). tranche14 = 12 tests. NOT yet wired: persistence projection/edge-function for the new tables (agreement events already flow), and portal UI (next increment).
- Master-data reshape v2 (APPROVED foundations for the 23-feature roadmap; skeleton only, no feature modules yet): four zero-dep kernel modules — src/party.ts (PartyDirectory: person/org parties + agreement_party role links resident/financial_responsible/guarantor/cosigner/occupant/prospect/payee; billTo() = payer-or-resident, so parents-pay-for-student needs no billing special-case), src/space.ts (SpaceTree: self-referencing property→building→unit→room→bed + common/amenity, leasable flag, ancestors/descendants/path), src/entity.ts (EntityCatalog: legal_entity operator/condominium/landlord/spe + charge_type routing a charge code → gl_account + receiving entity, so condo fees book to a different entity than rent), src/payables.ts (Payables: AP mirror of Billing — bill/bill_line/ap_payment posting DR expense/CR AP then DR AP/CR cash; a resident refund is just a bill to the resident-payee, #22). Migration 20260708170000_master_data_v2 APPLIED live (additive + backward-compatible): 8 new tables (party, agreement_party, space, legal_entity, charge_type, bill, bill_line, ap_payment) all deny-by-default+forced RLS; invoice.receiving_entity_id, invoice_line.charge_type, calendar_hold.space_id added nullable; agreement_event type check widened with party_assigned/party_released/rent_adjusted/transferred/moved_in/moved_out. unit + guest tables remain (view cutover deferred). NOT yet wired: projection/edge-function, API endpoints, portal, Agreement aggregate methods for the new events — those come with each feature. Review artifact: the approved data model. tranche13 = 12 tests.
- Runtime write path (autonomous persistence): `persist-world` Edge Function DEPLOYED and ACTIVE on shplrbhwpttsukwgaxli (verify_jwt=true, version 1). It runs INSIDE Supabase, reaching Postgres over the internal SUPABASE_DB_URL — this is the sanctioned way around the container's egress block (org policy denies TCP 5432 and HTTPS to supabase.co:443, so runtime `pg` from here is impossible; that limitation is environmental, not code). The function takes a domain-level WorldData, projects it FK-ordered server-side (no raw SQL from callers — inv 3), asserts journal balance app-side (inv 6), then applies the batch in ONE tx + `set constraints all immediate` (inv 4/6). Auth: gateway verify_jwt PLUS in-body role=service_role. Caller side is src/persistence/edge-client.ts (zero-dep fetch; persistWorldViaEdge → EdgePersistResult / throws EdgePersistError on non-200). Source: supabase/functions/persist-world/{index.ts,projection.ts,deno.json}. projection.ts is a verbatim twin of src/persistence/project.ts, drift-guarded byte-for-byte by tranche11. VERIFIED against the live schema: the kernel-projected probe batch (fresh 'edge-probe' tenant) applied cleanly, the deferred balance trigger validated (6 balanced lines), then ROLLED BACK (0 rows left, elara-hq intact). Cannot be invoked over HTTPS from THIS container (egress 403 + no service key here by design); it is the write arm for deployed infra.
- Public API wiring: the App accepts an injected `PersistenceBackend` (src/persistence/edge-client.ts: interface + `edgePersistenceBackend()` factory over persistWorldViaEdge). `App.snapshotWorld(tenantId)` folds in-memory state into a tenant-scoped, FK-parents-first, balanced WorldData (proven projection-ready + balanced by tranche12). `POST /persist` flushes it through the backend — same auth+RBAC gates as dispatch, new `persistence.run` permission (owner/service/manager). It is async I/O so it lives as `App.persist()` OFF the synchronous dispatch() router; http.ts routes POST /persist to it and awaits. No backend → 501; read_only → 403; backend EdgePersistError status is surfaced verbatim. HTTP round-trip smoke-tested in-process (401/200/backend-received-world).
- Phase 1 persistence: projectWorld + the edge twin now cover the v2 tables (legal_entity, party, space [topo-sorted parents-first], charge_type, agreement_party, bill/bill_line, ap_payment) plus invoice.receiving_entity_id and invoice_line.charge_type; agreement.guest_id made nullable (migration 20260708170500) so an agreement's resident can be carried purely by the party role (snapshotWorld emits guest_id only for a real master-data guest, else null). Edge Function REDEPLOYED to v3 (ACTIVE). VERIFIED live (rolled back): a full Phase-1 world — 3 parties, 2 spaces, charge type, 2 agreement roles, bill+line+ap_payment, charge-routed invoice (receiving_entity_id set), rent_adjusted event — applied cleanly to shplrbhwpttsukwgaxli. Incremental marks extended: bill_line is append-only (high-water by flushed bill ids), everything else upserts.
- Incremental sync: `/persist` sends only rows appended since the last SUCCESSFUL flush (per-tenant high-water mark in App.flushMarks — events-per-agreement, journal-line count, action-log count, flushed-invoice ids). The projection now upserts the state/parent tables (ON CONFLICT: tenant/unit/guest/rate_plan name/label/status fields; agreement DO NOTHING; calendar_hold/invoice/payment/deposit status) while the append-only streams (agreement_event, journal_line, action_log) + invoice_line are never re-sent. So repeated flushes are idempotent and reflect status changes (invoice paid, deposit refunded) without tripping the append-only triggers. Mark advances ONLY on success, so a failed flush safely re-sends. VERIFIED live: full flush + refund delta applied in sequence against shplrbhwpttsukwgaxli — 9 journal lines (no dup), 2 events (0 re-sent), 1 invoice_line (0 re-sent), deposit upserted held→refunded, trial balance 0, then ROLLED BACK. Edge Function REDEPLOYED to v2 (ACTIVE) with the upsert projection.
- Lifecycle acceptance: `npx tsx demo.ts` runs the full lifecycle in-process (green). `npx tsx demo-live.ts` projects the accumulated kernel state to SQL and persists it via the persistence adapter — P0 acceptance against the live stack is DONE: the projected batch was applied to shplrbhwpttsukwgaxli in a transaction, the deferred balance trigger validated via SET CONSTRAINTS ALL IMMEDIATE (trial balance 0), state verified (1 agreement, 3 events, 1 active hold, 6 journal lines, invoice paid, 6 action-log rows), then ROLLED BACK to leave the DB clean.
- Persistence adapter: src/persistence/ — zero-dep world projection (projectWorld) builds FK-ordered parameterized INSERTs; SqlExecutor boundary (RecordingExecutor for scripts/tests, optional PgExecutor over `pg` for the production service-role backend). demo-live uses PgExecutor when DATABASE_URL is set, else emits a runnable script.
- Migrations 20260707170000/1/2/3 APPLIED to Supabase project `shplrbhwpttsukwgaxli` ("Elara PMS", org `llqaczctlhlphhdmyeml` / jorquesa@icloud.com). The earlier "wrong org" issue was resolved by reconnecting the Supabase connector to the account that owns the project; shplrbhwpttsukwgaxli is the live DB target (NOT abandoned).
- DB-level invariants verified against the live DB: calendar_hold EXCLUDE rejects overlapping holds (inv 4); deferred balance trigger rejects unbalanced entries (inv 6); append-only trigger rejects UPDATE/DELETE on journal_line (inv 1). Seed loaded: 47 policy rules (regenerated via gen-seed after Phase 1 added agreement.adjust_rent/transfer + bill.issue/pay rules), 5 collection stages.
- RLS: deny-by-default on all 12 tenant tables (anon sees nothing until a tenant_id JWT claim exists) PLUS migration 3 locks journal_line + operational tables (policy_rule, collection_stage, action_log, exception_item) — RLS forced, no anon policy, service-role only.
- Security advisor: 0 ERROR/WARN of concern. Remaining = 6 INFO rls_enabled_no_policy (intentional deny-by-default) + 1 WARN btree_gist-in-public (left in place; the live EXCLUDE constraint depends on its opclass, moving it risks breaking inv 4).

## Layout
- src/ — domain kernel: agreement (state machine + conversion + Calendar), ledger, policy-envelope,
  agent-runtime, exception-queue, billing, payables (AP), rate-plan, payments, nfe-ingest, collections, multigaap,
  deposits, amenity, maintenance, reservations, inspection, communications, reconciliation, integrations, revenue (dynamic pricing + KPIs), procurement (purchase orders + budgets), roommate (student matching), onboarding (CSV import), crm (pipeline + KPIs), application (rental applications + screening), lease-doc (jurisdiction-aware lease document generator feeding e-sign), esign (lease e-signature), notifications (email/SMS outbox), observability (logger + metrics registry + error-reporter seam), ratelimit (token bucket), compliance (LGPD/GDPR subject-access + erasure), metrics (hospitality KPIs), group-block, party, space, entity (legal entity + charge catalog); src/persistence/ — executor + world projection (write) + repository (read: Agreement.rehydrate + Repositories.loadWorld [full DB→WorldData], tenant-scoped trial balance; PgQueryExecutor) + edge-client (persistWorldViaEdge → the deployed persist-world function)
- tests/ — twenty-one tranches (core, money, extended, persistence, api, agent, repository, platform, portal, billing-view, edge, persist-api, master-data-v2, phase1, maintenance, reservations, moveinout, communications, reconciliation, integrations, revenue, procurement, roommate, onboarding, crm, esign, country, rehydrate, agent-tools) + fixtures (NF-e XML)
- schema.sql — Postgres persistence design (mirrored as migration 20260707170000)
- supabase/migrations/ — 14 migrations (core schema, policy seed, RLS, RLS operational tables, master_data_v2, agreement_guest_nullable, work_order, phase2_tables, pms_feature_tables, config_and_full_persistence, master_data_fidelity, notifications, journal_line_tenant, schedulers); supabase/policy_rule.seed.sql generated seed; scripts/gen-seed.mjs regenerates from TS; scripts/provision-country.mjs <CC> emits a country environment's full setup
- supabase/functions/persist-world/ — Deno Edge Function: runtime write arm hosted inside Supabase (index.ts handler, projection.ts = verbatim twin of src/persistence/project.ts, deno.json). Deploy via the Supabase MCP `deploy_edge_function`; keep verify_jwt=true. supabase/functions/connector-worker/ — drains the connector outbox (vendor I/O). supabase/functions/notification-worker/ — drains the notification outbox (email via SendGrid / SMS via Twilio, credential from Deno.env). All three: verify_jwt=true + in-body role=service_role.
- docs/unified-stay-os-spec.md — architecture; §13 gates RESOLVED; §14 Public API, §15 agent tools, §16 read layer, §17 platform (config/i18n/rbac/master-data), §18 operator portal (all P1).
- src/api/ — Public API: context (auth), app (dispatch router + 3 gates: auth → RBAC permission → policy; plus async App.persist()/snapshotWorld() for durable flush). App now also owns parties (PartyDirectory), spaces (SpaceTree), entities (EntityCatalog), payables (Payables). Endpoints: agreements (+adjust-rent/transfer), invoices (charge-routed), payments, deposits, parties (+per-agreement roles), spaces, legal-entities, charge-types, bills (+pay), pricing-rules (+quote), revenue/summary, purchase-orders (+approve/receive/close/cancel), budgets (+status), prospects (+matches), roommate/grouping, onboarding/{preview,commit}, leads (+advance/lose), crm/summary, signature-envelopes (+send/sign/void), countries, environment, ledger, exceptions, reporting. http (node:http bind, serves portal at /, routes POST /persist → App.persist). src/agent/tools.ts — AgentToolCatalog (~31 Anthropic-tool-use specs spanning the whole platform → App.dispatch, policy-gated).
- Platform layer: src/country.ts (COUNTRY_PROFILES: a country environment fixing currency/locale/timezone/jurisdiction/taxIdLabel), src/environment.ts (MASTER_DATA_STRUCTURE constant + buildEnvironment → per-country blueprint over the identical structure; effectiveRulesFor slices policy per jurisdiction), src/config.ts (TenantConfig: country+jurisdiction+locale/currency w/ minor units/timezone/businessStructure; jurisdiction derived from country; formatMoney; setup-time switchable, flows into ledger + policy), src/i18n.ts (en/pt-BR/es), src/rbac.ts (granular permissions + built-in + custom roles; orthogonal to policy envelope), src/master-data.ts (units/guests/users/rate-plans, tenant-scoped, code = reporting key), src/subscription.ts (per-unit SaaS). ALL platform + feature state is now DB-backed (tenant config columns + app_user/custom_role/integration/connector_command/signature_envelope tables); nothing lives only in memory.
- Portal: src/api/portal.html — zero-dep vanilla SPA, brand mark + light/dark themes (setup wizard, dashboard, agreements + agreement-detail with billing lifecycle: issue invoice / record payment / hold+refund deposit + event-history timeline, ledger, users&roles). Phase 1 additions: a Bills (accounts-payable) nav view — record a vendor bill against a party payee and pay it (large payouts escalate); and on the agreement detail a People section (assign resident/financial_responsible/guarantor… with bill-to shown) plus Adjust-rent (percent/fixed) and Transfer actions. `npx tsx demo-portal.ts` (4 demo tokens by role). Browser QA: `npm run portal:drive` (playwright-core) — drives the new People/adjust-rent/Bills flows too (NO CONSOLE ERRORS). GET /agreements/:id/billing backs the detail view.

## Commands
- Setup: `npm i -D tsx typescript @types/node`
- Tests: `npx tsx --test tests/*.test.ts` (must be 428/428 before any commit)
- Typecheck: `npx tsc --noEmit`
- Demo (in-process): `npx tsx demo.ts`
- Demo (live persist): `DATABASE_URL=… npx tsx demo-live.ts` (no URL → emits SQL script)
- Seed regen: `npx tsx scripts/gen-seed.mjs`

## Behavioral guardrails for you (Claude Code)
- Stage 0 of CLAUDE-CODE-P0-PROMPT.md: restate the invariants back before writing code.
- Conversion logic (nightly→monthly→lease) preserves agreement id and ledger continuity — regression here is critical.
- Never write payment-provider code touching real credentials without explicit human approval in-session.
- Eviction, lease execution in BR/EU, and any irreversible+regulated action: propose to human, never execute.
- Commit after each green subtask; long sessions: /clear and rely on this file to reload context.
